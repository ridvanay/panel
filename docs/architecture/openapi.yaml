openapi: 3.0.3
info:
  title: SaaS Platform API
  version: "1.0"
  description: >
    Frontend (Next.js) ve backend (Node.js/Fastify) ajanlarının uyacağı API
    kontratı. Kaynak doğruluk: bu dosya + ../shared-types.ts + ARCHITECTURE.md.
servers:
  - url: https://api.example.com/api/v1
    description: Production
  - url: http://localhost:4000/api/v1
    description: Local

tags:
  - name: Auth
  - name: Users
  - name: Organizations
  - name: Members
  - name: Invitations
  - name: Plans
  - name: Billing
  - name: Webhooks
  - name: Health
  - name: AdminUsers
    description: Org'dan bağımsız site-geneli kullanıcı/rol yönetimi (`/admin/users`) — yalnızca SiteRole=ADMIN.
  - name: Logs
    description: Audit log (`/admin/logs`) — yalnızca SiteRole=ADMIN.
  - name: Navigation
    description: >
      Site header/nav/footer yapılandırması (`/navigation`, `/admin/navigation`).
      `/navigation` public'tir (site header/nav/footer bunu okur); `/admin/navigation`
      yönetim ekranı içindir, PUT yalnızca SiteRole=ADMIN.
  - name: Pages
    description: >
      CMS sayfa yönetimi (`/admin/pages`). Okuma authenticated, yazma SiteRole=ADMIN
      veya EDITOR, kalıcı silme yalnızca ADMIN. Soft-delete (çöp kutusu), toplu işlem
      ve SEO tamlık skoru için bkz. ARCHITECTURE.md §10.7.
  - name: Blog
    description: >
      Blog yazısı yönetimi (`/admin/blog`). Yetki eşikleri ve çöp/toplu işlem/SEO skoru
      davranışı `Pages` ile BİREBİR AYNIDIR — bkz. ARCHITECTURE.md §10.7.
  - name: ContentRevisions
    description: >
      İçerik sürüm kontrolü (`/admin/pages/{pageId}/revisions`,
      `/admin/blog/{postId}/revisions`) — bkz. ARCHITECTURE.md §10.1. Yetki eşiği,
      ilgili entity'nin (pages/blog) yazma eşiğiyle aynı (SiteRole=ADMIN veya EDITOR).
  - name: EmailTemplates
    description: >
      E-posta & bildirim şablonu yönetimi (`/admin/notifications/templates`) — bkz.
      ARCHITECTURE.md §10.3. Tüm uçlar yalnızca SiteRole=ADMIN.
  - name: Import
    description: >
      Toplu içe aktarma (`/admin/import/*`) — bkz. ARCHITECTURE.md §10.8. Sayfa/blog
      (CSV+JSON), WordPress WXR (XML), kullanıcı (CSV) ve medya (ZIP) içe aktarımı.

      **TÜM uçlar İSTİSNASIZ yalnızca SiteRole=ADMIN'dir** — içerik yazma eşiği normalde
      ADMIN+EDITOR olsa da (bkz. `Pages`/`Blog`), toplu içe aktarma kayıt-bazlı incelemeyi
      atlar, mevcut içeriğin ÜZERİNE yazabilir, keyfi `authorId` atayabilir ve (kullanıcı
      içe aktarımında) hesap oluşturup rol verebilir. EDITOR/VIEWER → 403.

      **Dışa aktarma için uç YOKTUR (karar):** CSV dışa aktarma tamamen client-side'dır
      (`frontend/src/lib/export-csv.ts::exportToCsv`) — admin listeleri zaten tüm kayıtları
      `meta.nextCursor` döngüsüyle belleğe çeker, ayrı bir sunucu ucu ikinci bir doğruluk
      kaynağı yaratırdı. Bkz. ARCHITECTURE.md §10.8 "Dışa aktarma". (Bu, `Pages`/`Blog`/
      `AdminUsers` LİSTE ekranlarının CSV dışa aktarımı içindir — analitik/raporlama dışa
      aktarımı için bkz. `Reports` tag'i, `/admin/reports/exports`.)
  - name: Stats
    description: >
      Admin analitik/raporlama verisi (`/admin/stats/*`) — bkz. ARCHITECTURE.md §10.8.10.
      RBAC UÇ-BAZLIDIR (modül genelinde tek bir eşik YOKTUR): içerik analitiği (`/views`,
      `/breakdown`, `/top-content`, `/live-visitors`) SiteRole=ADMIN veya EDITOR; kullanıcı/
      gelir verisi (`/summary`, `/users`, `/revenue`) yalnızca SiteRole=ADMIN (PII/gelir
      riski nedeniyle).
  - name: Reports
    description: >
      Analitik rapor dışa aktarma (`/admin/reports/exports`) — bkz. ARCHITECTURE.md
      §10.8.10. **TÜM uçlar İSTİSNASIZ yalnızca SiteRole=ADMIN'dir** (rapor türü fark
      etmeksizin — `Stats`'ın `/summary|/users|/revenue` kısıtlamasıyla AYNI gerekçe, ama
      burada tip ayrımı bile YAPILMAZ). Süreç-içi worker ile asenkron üretilir (kuyruk
      altyapısı yok, §10.8.1 ile aynı desen); oluşturma ucu 10 istek/10 dakika ile
      sınırlıdır (429).

security:
  - bearerAuth: []

paths:
  /healthz:
    get:
      tags: [Health]
      security: []
      summary: Servis sağlık kontrolü
      responses:
        "200":
          description: Servis ayakta
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: ok }

  /auth/register:
    post:
      tags: [Auth]
      security: []
      summary: Yeni kullanıcı kaydı
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RegisterRequest" }
      responses:
        "201":
          description: Kayıt başarılı, oturum açıldı
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/AuthResponse" }
        "422": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Conflict" }

  /auth/login:
    post:
      tags: [Auth]
      security: []
      summary: E-posta/şifre ile giriş
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LoginRequest" }
      responses:
        "200":
          description: Giriş başarılı
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/AuthResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /auth/refresh:
    post:
      tags: [Auth]
      summary: Refresh cookie ile yeni access token al (rotasyonlu)
      security: []
      description: Refresh token httpOnly cookie üzerinden okunur, body gerekmez.
      responses:
        "200":
          description: Yeni access token
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/AuthTokens" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /auth/logout:
    post:
      tags: [Auth]
      summary: Oturumu kapat (refresh token'ı iptal et)
      responses:
        "204": { description: Çıkış yapıldı }

  /auth/forgot-password:
    post:
      tags: [Auth]
      security: []
      summary: Şifre sıfırlama e-postası gönder
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email: { type: string, format: email }
      responses:
        "202": { description: İstek alındı (e-posta enumeration önlemek için her zaman 202) }

  /auth/reset-password:
    post:
      tags: [Auth]
      security: []
      summary: Şifreyi token ile sıfırla
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token, newPassword]
              properties:
                token: { type: string }
                newPassword: { type: string, minLength: 8 }
      responses:
        "204": { description: Şifre güncellendi }
        "400": { $ref: "#/components/responses/ValidationError" }

  /auth/me:
    get:
      tags: [Auth]
      summary: Mevcut oturumun kullanıcı ve üyelik bilgisi
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/AuthSession" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /users/me:
    get:
      tags: [Users]
      summary: Profilimi getir
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/User" }
    patch:
      tags: [Users]
      summary: Profilimi güncelle
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateUserRequest" }
      responses:
        "200":
          description: Güncellendi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/User" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /users/me/change-password:
    post:
      tags: [Users]
      summary: Kendi şifremi değiştir
      description: >
        Oturum açmış kullanıcı KENDİ şifresini değiştirir (site-rol şartı YOK, `authenticate`
        yeterli). `currentPassword` argon2 ile doğrulanır; hatalıysa 401 döner ve
        `security.password_change` audit kaydı `status: FAILURE` ile yazılır. Başarılı olursa
        `passwordHash` güncellenir ve **mevcut oturum hariç** tüm `RefreshToken` kayıtları
        iptal edilir (bkz. `/admin/settings/security/2fa/disable` ile aynı politika) —
        yani diğer cihazlardaki oturumlar zorla kapanır, isteği yapan cihaz açık kalır.
        Hassas uç: route-level rate limit uygulanır (5 istek / 1 dakika).
        Şifre sıfırlama (token ile, oturumsuz) akışı için bkz. `/auth/reset-password`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ChangePasswordRequest" }
      responses:
        "204": { description: Şifre değiştirildi (gövde yok) }
        "401":
          description: >
            `currentPassword` hatalı veya oturum geçersiz. Mesaj, hangi alanın hatalı
            olduğunu sızdırmayacak şekilde sabittir ("Şifre hatalı.").
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }
        "422": { $ref: "#/components/responses/ValidationError" }
        "429": { $ref: "#/components/responses/TooManyRequests" }

  /organizations:
    get:
      tags: [Organizations]
      summary: Üyesi olduğum organizasyonları listele
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Organization" }
    post:
      tags: [Organizations]
      summary: Yeni organizasyon oluştur (oluşturan OWNER olur)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateOrganizationRequest" }
      responses:
        "201":
          description: Oluşturuldu
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/Organization" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /organizations/{orgId}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Organizations]
      summary: Organizasyon detayı
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/Organization" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      tags: [Organizations]
      summary: Organizasyonu güncelle (ADMIN/OWNER)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateOrganizationRequest" }
      responses:
        "200":
          description: Güncellendi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/Organization" }
        "403": { $ref: "#/components/responses/Forbidden" }
    delete:
      tags: [Organizations]
      summary: Organizasyonu sil (yalnızca OWNER)
      responses:
        "204": { description: Silindi }
        "403": { $ref: "#/components/responses/Forbidden" }

  /organizations/{orgId}/members:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Members]
      summary: Organizasyon üyelerini listele
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Membership" }

  /organizations/{orgId}/members/{userId}:
    parameters:
      - $ref: "#/components/parameters/OrgId"
      - name: userId
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      tags: [Members]
      summary: Üye rolünü değiştir (ADMIN/OWNER)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateMembershipRequest" }
      responses:
        "200":
          description: Güncellendi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/Membership" }
        "403": { $ref: "#/components/responses/Forbidden" }
    delete:
      tags: [Members]
      summary: Üyeyi organizasyondan çıkar (ADMIN/OWNER)
      responses:
        "204": { description: Çıkarıldı }
        "403": { $ref: "#/components/responses/Forbidden" }

  /organizations/{orgId}/invitations:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Invitations]
      summary: Bekleyen davetleri listele (ADMIN/OWNER)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Invitation" }
    post:
      tags: [Invitations]
      summary: Yeni üye davet et (ADMIN/OWNER)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateInvitationRequest" }
      responses:
        "201":
          description: Davet gönderildi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/Invitation" }
        "409": { $ref: "#/components/responses/Conflict" }

  /invitations/{token}/accept:
    post:
      tags: [Invitations]
      summary: Daveti kabul et (giriş yapmış kullanıcı)
      parameters:
        - name: token
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Kabul edildi, üyelik oluşturuldu
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/Membership" }
        "410": { description: Davet süresi dolmuş/iptal edilmiş }

  /plans:
    get:
      tags: [Plans]
      security: []
      summary: Herkese açık plan listesi
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Plan" }

  /organizations/{orgId}/subscription:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    get:
      tags: [Billing]
      summary: Organizasyonun abonelik durumu
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/Subscription" }
        "404": { $ref: "#/components/responses/NotFound" }

  /organizations/{orgId}/subscription/checkout-session:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    post:
      tags: [Billing]
      summary: Stripe Checkout oturumu oluştur (OWNER)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateCheckoutSessionRequest" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/CheckoutSessionResponse" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /organizations/{orgId}/subscription/portal-session:
    parameters:
      - $ref: "#/components/parameters/OrgId"
    post:
      tags: [Billing]
      summary: Stripe Billing Portal oturumu oluştur (OWNER)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/BillingPortalResponse" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /admin/users:
    get:
      tags: [AdminUsers]
      summary: Site kullanıcılarını listele (yalnızca ADMIN)
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AdminUser" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [AdminUsers]
      summary: Yeni site kullanıcısı oluştur (yalnızca ADMIN)
      description: >
        Kullanılamaz rastgele bir şifreyle oluşturulur; kullanıcı gerçek şifresini
        dönen `setPasswordUrl` üzerinden belirler (MVP/dev-only: e-posta sağlayıcısı
        entegre edilene kadar bağlantı doğrudan response'ta döner).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateAdminUserRequest" }
      responses:
        "201":
          description: Oluşturuldu
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/CreateAdminUserResponse" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/users/{userId}/role:
    parameters:
      - name: userId
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      tags: [AdminUsers]
      summary: Kullanıcının rolünü değiştir (yalnızca ADMIN)
      description: >
        Hedef kullanıcı sistemdeki tek aktif ADMIN ise ve yeni rol ADMIN değilse
        409 CONFLICT döner ("en az bir yönetici kalmalı" kuralı).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateAdminUserRoleRequest" }
      responses:
        "200":
          description: Güncellendi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/AdminUser" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/users/{userId}/status:
    parameters:
      - name: userId
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      tags: [AdminUsers]
      summary: Kullanıcının durumunu (ACTIVE/SUSPENDED) değiştir (yalnızca ADMIN)
      description: >
        Kendi hesabınızı askıya alamazsınız (her zaman 409). Hedef kullanıcı
        sistemdeki tek aktif ADMIN ise ve yeni durum SUSPENDED ise de 409 döner.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateAdminUserStatusRequest" }
      responses:
        "200":
          description: Güncellendi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/AdminUser" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/logs:
    get:
      tags: [Logs]
      summary: Audit log kayıtlarını listele (yalnızca ADMIN)
      description: >
        SAPMA: diğer tüm sayfalanan listelerin aksine en yeni kayıt önce gelir
        (`orderBy seq desc` + `seq < cursor`), diğer uçlarda `orderBy seq asc` + `seq > cursor`.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [SUCCESS, FAILURE, FORBIDDEN] }
        - name: action
          in: query
          required: false
          description: Öneke göre filtreler (ör. "user." → user.create, user.role_change, ...).
          schema: { type: string }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AuditLog" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /admin/settings/permissions:
    get:
      tags: [AdminUsers]
      summary: Statik rol/modül/aksiyon yetki matrisini getir (yalnızca ADMIN)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/PermissionsMatrix" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /navigation:
    get:
      tags: [Navigation]
      security: []
      summary: Yayındaki navigasyon/header/footer yapılandırmasını getir (public)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/NavigationConfig" }

  /admin/navigation:
    get:
      tags: [Navigation]
      summary: Navigasyon/header/footer yapılandırmasını getir (yönetim ekranı)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/NavigationConfig" }
    put:
      tags: [Navigation]
      summary: Navigasyon/header/footer yapılandırmasını değiştir (yalnızca ADMIN)
      description: >
        Tam değiştirme (replace) semantiği: `navigationItems`/`socialLinks`/`footerColumns`
        gönderilen dizilerle DB'deki mevcut kayıtların tamamının yerini alır (delete-then-recreate,
        tek transaction içinde).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateNavigationConfigRequest" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/NavigationConfig" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/stats/views:
    get:
      tags: [Stats]
      summary: Günlük sayfa/yazı görüntülenme zaman serisi (SiteRole=ADMIN veya EDITOR)
      description: >
        `granularity` bucket boyutunu belirler (varsayılan `day`). `compare` bu uçta
        KABUL EDİLİR ama YOK SAYILIR — karşılaştırma deltası yalnızca `GET .../summary`
        yanıtında döner.
      parameters:
        - $ref: "#/components/parameters/StatsDays"
        - $ref: "#/components/parameters/StatsFrom"
        - $ref: "#/components/parameters/StatsTo"
        - $ref: "#/components/parameters/StatsGranularity"
        - $ref: "#/components/parameters/StatsCompare"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DailyViewStats" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/stats/live-visitors:
    get:
      tags: [Stats]
      summary: >
        Süreç-içi (in-memory) canlı ziyaretçi sayısı (SiteRole=ADMIN veya EDITOR) —
        son 60 saniye içinde bir görüntülenme (view) endpoint'i çağıran benzersiz
        IP+User-Agent sayısı. Dağıtık DEĞİLDİR: çoklu instance dağıtımda her process
        kendi sayacını tutar.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/LiveVisitors" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /admin/stats/breakdown:
    get:
      tags: [Stats]
      summary: >
        Belirtilen aralıktaki görüntülenmelerin cihaz türü ve ülkeye göre kırılımı
        (SiteRole=ADMIN veya EDITOR)
      description: >
        `compare` bu uçta KABUL EDİLİR ama YOK SAYILIR (bkz. `/views` açıklaması).
      parameters:
        - $ref: "#/components/parameters/StatsDays"
        - $ref: "#/components/parameters/StatsFrom"
        - $ref: "#/components/parameters/StatsTo"
        - $ref: "#/components/parameters/StatsGranularity"
        - $ref: "#/components/parameters/StatsCompare"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/ViewsBreakdown" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/stats/top-content:
    get:
      tags: [Stats]
      summary: En çok görüntülenen sayfa/yazılar, azalan sırayla (SiteRole=ADMIN veya EDITOR)
      description: >
        Cursor sayfalı. `contentType` alanı `page`/`post` ayrımını verir, `id`/`slug`
        ilgili `Page`/`BlogPost` kaydına aittir.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/StatsDays"
        - $ref: "#/components/parameters/StatsFrom"
        - $ref: "#/components/parameters/StatsTo"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/TopContentItem" }
                      meta: { $ref: "#/components/schemas/TopContentListMeta" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/stats/summary:
    get:
      tags: [Stats]
      summary: Dönem özeti — görüntülenme/kullanıcı/gelir tek bakışta (yalnızca SiteRole=ADMIN)
      description: >
        `activeSubscriptions`/`mrrCents` ANLIK durumu yansıtır (dönem sonuna göre DEĞİL) —
        `Subscription` geçmiş durum geçmişi tutan bir tablo olmadığı için bu iki alan için
        `compare` BİLEREK dönülmez (bkz. `SummaryStats.compare` alan açıklaması).
        `compare=true` verilirse `pageViews`/`postViews`/`newUsers` için bir önceki
        eş-uzunluktaki dönemle karşılaştırma deltası `compare` alanında döner; verilmezse
        `compare: null`.
      parameters:
        - $ref: "#/components/parameters/StatsDays"
        - $ref: "#/components/parameters/StatsFrom"
        - $ref: "#/components/parameters/StatsTo"
        - $ref: "#/components/parameters/StatsGranularity"
        - $ref: "#/components/parameters/StatsCompare"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/SummaryStats" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/stats/users:
    get:
      tags: [Stats]
      summary: Yeni kullanıcı zaman serisi + anlık rol dağılımı (yalnızca SiteRole=ADMIN)
      description: >
        `compare` bu uçta KABUL EDİLİR ama YOK SAYILIR (bkz. `/admin/stats/views`
        açıklaması — yalnızca `/summary` karşılaştırma deltası döner). `roleDistribution`
        dönemle SINIRLI DEĞİLDİR — kullanıcı tablosunun ANLIK rol dağılımıdır.
      parameters:
        - $ref: "#/components/parameters/StatsDays"
        - $ref: "#/components/parameters/StatsFrom"
        - $ref: "#/components/parameters/StatsTo"
        - $ref: "#/components/parameters/StatsGranularity"
        - $ref: "#/components/parameters/StatsCompare"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/UsersStats" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/stats/revenue:
    get:
      tags: [Stats]
      summary: Gelir zaman serisi (yeni MRR/churn) + anlık MRR/aktif abonelik (yalnızca SiteRole=ADMIN)
      description: >
        `compare` bu uçta KABUL EDİLİR ama YOK SAYILIR (bkz. `/admin/stats/views`
        açıklaması). `activeSubscriptions`/`mrrCents` ANLIK toplamlardır (dönem sonuna
        göre DEĞİL). `series[].newMrrCents` bucket içinde OLUŞTURULAN aboneliklerin
        `priceMonthlyCents` toplamıdır; `series[].churnedCount` bucket içinde `CANCELED`
        durumuna geçen (yaklaşık — `updatedAt` referans alınır) abonelik sayısıdır.
        Yıllık abonelikler ay karşılığına ÇEVRİLMEZ, olduğu gibi `priceMonthlyCents`
        kullanılır.
      parameters:
        - $ref: "#/components/parameters/StatsDays"
        - $ref: "#/components/parameters/StatsFrom"
        - $ref: "#/components/parameters/StatsTo"
        - $ref: "#/components/parameters/StatsGranularity"
        - $ref: "#/components/parameters/StatsCompare"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/RevenueStats" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/health:
    get:
      tags: [System]
      summary: Sistem sağlığı — DB ping/boyutu, medya depolama, sunucu belleği/yükü (yalnızca ADMIN)
      description: >
        Tüm alanlar gerçek zamanlı ölçülür (DB `SELECT 1` ping süresi, `pg_database_size`,
        `Media.sizeBytes` toplamı, Node `os`/`process` bellek-yük-uptime bilgisi) — sabit/uydurma
        değer içermez. `dbQuotaBytes`/`mediaStorageQuotaBytes`, ilgili ortam değişkeni
        (`DB_STORAGE_QUOTA_MB`/`MEDIA_STORAGE_QUOTA_MB`) tanımlı değilse `null` döner.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/SystemHealth" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /admin/pages:
    get:
      tags: [Pages]
      summary: Sayfaları listele — çöp filtresi + sekme sayaçları (authenticated)
      description: >
        **Çöp kutusu (soft-delete) semantiği:** varsayılan `trashed=exclude`, yani
        `deletedAt IS NULL` olan kayıtlar döner. `trashed=include` çöptekiler DAHİL
        tümünü, `trashed=only` yalnızca çöptekileri döner.


        **Sekme sayaçları:** `meta.counts` HER ZAMAN döner ve `trashed`/`status`/
        `cursor`/`limit` parametrelerinden ETKİLENMEZ — tablo genelinde tek bir
        `groupBy` ile hesaplanır. Frontend'in 4 sekmesinin (Tümü/Yayınlanmış/Taslak/Çöp)
        rakamları BURADAN okunur; client-side sayım YAPILMAZ (100'lük sayfa limitinin
        ötesinde yanlış sonuç verirdi).


        **v1 frontend kullanımı:** `?trashed=include&limit=100` ile çekilir, satır
        filtreleme/arama/sayfalama client-side yapılır (`useFilteredList`). Toplam kayıt
        100'ü aşarsa frontend `meta.nextCursor` boşalana kadar döngüyle devam sayfalarını
        çeker (`counts.all + counts.trashed` beklenen toplamı verir). `status` parametresi
        API tüketicileri ve ileride sunucu-taraflı sayfalamaya geçiş için vardır;
        v1 frontend'i KULLANMAZ.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/TrashedFilter"
        - $ref: "#/components/parameters/ContentStatusFilter"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Page" }
                      meta: { $ref: "#/components/schemas/ContentListMeta" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [Pages]
      summary: Yeni sayfa oluştur (ADMIN/EDITOR)
      description: >
        `slug` verilmezse `title`'dan üretilir. `authorId` verilmezse giriş yapmış
        kullanıcı yazar olur; BAŞKA bir kullanıcıyı yazar atamak yalnızca ADMIN'e
        açıktır (EDITOR gönderirse 403).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePageRequest" }
      responses:
        "201":
          description: Sayfa oluşturuldu
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/Page" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/pages/{pageId}:
    parameters:
      - $ref: "#/components/parameters/PageId"
    get:
      tags: [Pages]
      summary: Sayfa detayı (authenticated)
      description: >
        Çöpteki sayfayı da döner (geri yükleme/onay ekranları için) — çağıran taraf
        `deletedAt` alanına bakarak davranışını belirler.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/Page" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      tags: [Pages]
      summary: Sayfayı güncelle — tam düzenleme VE hızlı düzenleme (ADMIN/EDITOR)
      description: >
        **Hızlı Düzenle (Quick Edit) için AYRI UÇ YOKTUR** — liste ekranındaki inline
        mini form da bu ucu kısmi gövdeyle çağırır: `{ title?, slug?, status? }`.
        Böylece slug tekilliği, revizyon snapshot'ı ve `publishedAt` mantığı tek yerde kalır.


        Kurallar:

        - Her başarılı PATCH, güncellemeden önceki hali `ContentRevision` olarak kaydeder (§10.1).

        - Çöpteki bir sayfa DÜZENLENEMEZ → `409 CONFLICT` (`"Çöpteki içerik düzenlenemez.
        Önce geri yükleyin."`). Önce `POST .../restore`.

        - `status: PUBLISHED` ve `publishedAt` boşsa `publishedAt = now()` set edilir.
        Taslağa geri alma `publishedAt`'i TEMİZLEMEZ (ilk yayın tarihi kalıcıdır).

        - `authorId` alanını yalnızca ADMIN değiştirebilir; EDITOR gönderirse 403.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePageRequest" }
      responses:
        "200":
          description: Güncellendi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/Page" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: >
            Slug çakışması VEYA çöpteki içeriği düzenleme girişimi
            (`error.code: CONFLICT`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }
        "422": { $ref: "#/components/responses/ValidationError" }
    delete:
      tags: [Pages]
      summary: Sayfayı ÇÖPE TAŞI — soft delete (ADMIN/EDITOR)
      description: >
        **DAVRANIŞ DEĞİŞİKLİĞİ:** bu uç artık kalıcı silmez, `deletedAt = now()` set eder.
        Kalıcı silme için `DELETE /admin/pages/{pageId}/permanent` (yalnızca ADMIN).


        - İdempotenttir: zaten çöpte olan sayfa için de 204 döner.

        - Çöpe taşınan sayfa `SiteSettings.homePageId` ise, aynı transaction içinde
        `homePageId = null` yapılır (public site kırılmasın). Geri yükleme bunu GERİ ALMAZ.

        - Yetki eşiği ADMIN-only'den ADMIN/EDITOR'e GENİŞLETİLMİŞTİR (çöp geri
        alınabilir bir işlemdir); kalıcı silme ADMIN-only kalır.
      responses:
        "204": { description: Çöpe taşındı }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/pages/{pageId}/restore:
    parameters:
      - $ref: "#/components/parameters/PageId"
    post:
      tags: [Pages]
      summary: Sayfayı çöpten geri yükle (ADMIN/EDITOR)
      description: >
        `deletedAt = null` yapar. `status` DEĞİŞTİRİLMEZ — sayfa çöpe taşındığındaki
        durumuna (DRAFT/PUBLISHED) geri döner. İdempotenttir: çöpte olmayan sayfa için
        de 200 ve mevcut kayıt döner.
      responses:
        "200":
          description: Geri yüklendi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/Page" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/pages/{pageId}/permanent:
    parameters:
      - $ref: "#/components/parameters/PageId"
    delete:
      tags: [Pages]
      summary: Sayfayı KALICI sil — geri alınamaz (yalnızca ADMIN)
      description: >
        Güvenlik bariyeri: sayfa ÖNCE çöpte olmalıdır. `deletedAt IS NULL` ise
        `409 CONFLICT` (`"Kalıcı silmeden önce içeriği çöpe taşıyın."`).


        Aynı transaction içinde temizlenmesi ZORUNLU yan kayıtlar:

        - `ContentRevision` satırları (polymorphic, FK YOK → elle silinmeli:
        `entityType = PAGE AND entityId = {pageId}`).

        - `PageView` satırları FK cascade ile otomatik silinir.

        - `SiteSettings.homePageId` FK `onDelete: SetNull` ile otomatik boşalır.

        - `AuditLog` kaydı yazılır (`page.permanent_delete`).
      responses:
        "204": { description: Kalıcı olarak silindi }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "İçerik çöpte değil (`error.code: CONFLICT`)"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }

  /admin/pages/bulk:
    post:
      tags: [Pages]
      summary: Sayfalarda toplu işlem (ADMIN/EDITOR; permanent-delete yalnızca ADMIN)
      description: >
        Liste ekranındaki toplu işlem menüsü ve "Çöpü Boşalt" bu ucu kullanır —
        ayrı bir "çöpü boşalt" ucu YOKTUR (frontend çöpteki id'leri
        `action: permanent-delete` ile gönderir).


        **Kısmi başarı bir hata DEĞİLDİR:** uygulanamayan id'ler `skippedIds`'e düşer
        ve yanıt yine 200 olur. Atlama sebepleri:

        - id bulunamadı,

        - `trash` → zaten çöpte,

        - `restore` → zaten çöpte değil,

        - `publish`/`draft` → kayıt çöpte (önce geri yüklenmeli),

        - `permanent-delete` → kayıt çöpte değil.


        `permanent-delete` yalnızca ADMIN içindir; EDITOR gönderirse tüm istek 403 olur
        (kısmi uygulama yapılmaz). Her toplu işlem tek bir `AuditLog` kaydı yazar
        (`page.bulk_<action>`, metadata'da id listesi).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BulkContentActionRequest" }
      responses:
        "200":
          description: İşlem uygulandı (kısmi başarı dahil)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/BulkContentActionResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/blog:
    get:
      tags: [Blog]
      summary: Blog yazılarını listele — çöp filtresi + sekme sayaçları (authenticated)
      description: >
        Davranış `GET /admin/pages` ile BİREBİR AYNI (çöp semantiği, `meta.counts`,
        v1 frontend kullanımı) — oradaki açıklama bağlayıcıdır.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/TrashedFilter"
        - $ref: "#/components/parameters/ContentStatusFilter"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/BlogPost" }
                      meta: { $ref: "#/components/schemas/ContentListMeta" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [Blog]
      summary: Yeni blog yazısı oluştur (ADMIN/EDITOR)
      description: >
        `authorId` kuralı `POST /admin/pages` ile aynıdır: verilmezse giriş yapmış
        kullanıcı, başka kullanıcı atamak yalnızca ADMIN'e açıktır.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateBlogPostRequest" }
      responses:
        "201":
          description: Yazı oluşturuldu
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/BlogPost" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/blog/{postId}:
    parameters:
      - $ref: "#/components/parameters/PostId"
    get:
      tags: [Blog]
      summary: Blog yazısı detayı (authenticated) — çöptekiler dahil
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/BlogPost" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      tags: [Blog]
      summary: Yazıyı güncelle — tam düzenleme VE hızlı düzenleme (ADMIN/EDITOR)
      description: >
        Kurallar `PATCH /admin/pages/{pageId}` ile BİREBİR AYNI — Hızlı Düzenle için ayrı
        uç yoktur, kısmi gövde `{ title?, slug?, status? }` bu uca gönderilir; çöpteki
        yazı düzenlenemez (409).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateBlogPostRequest" }
      responses:
        "200":
          description: Güncellendi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/BlogPost" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: >
            Slug çakışması VEYA çöpteki içeriği düzenleme girişimi
            (`error.code: CONFLICT`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }
        "422": { $ref: "#/components/responses/ValidationError" }
    delete:
      tags: [Blog]
      summary: Yazıyı ÇÖPE TAŞI — soft delete (ADMIN/EDITOR)
      description: >
        `DELETE /admin/pages/{pageId}` ile aynı davranış (idempotent, `deletedAt = now()`,
        yetki eşiği ADMIN/EDITOR). BlogPost'un `SiteSettings` bağı YOKTUR.
      responses:
        "204": { description: Çöpe taşındı }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/blog/{postId}/restore:
    parameters:
      - $ref: "#/components/parameters/PostId"
    post:
      tags: [Blog]
      summary: Yazıyı çöpten geri yükle (ADMIN/EDITOR)
      responses:
        "200":
          description: Geri yüklendi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/BlogPost" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/blog/{postId}/permanent:
    parameters:
      - $ref: "#/components/parameters/PostId"
    delete:
      tags: [Blog]
      summary: Yazıyı KALICI sil — geri alınamaz (yalnızca ADMIN)
      description: >
        `DELETE /admin/pages/{pageId}/permanent` ile aynı kural: yazı ÖNCE çöpte olmalı,
        değilse 409. Aynı transaction'da `ContentRevision` satırları
        (`entityType = BLOG_POST AND entityId = {postId}`) elle silinir; `PageView`
        cascade ile düşer; `AuditLog` (`blog_post.permanent_delete`) yazılır.
      responses:
        "204": { description: Kalıcı olarak silindi }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "İçerik çöpte değil (`error.code: CONFLICT`)"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }

  /admin/blog/bulk:
    post:
      tags: [Blog]
      summary: Blog yazılarında toplu işlem (ADMIN/EDITOR; permanent-delete yalnızca ADMIN)
      description: >
        `POST /admin/pages/bulk` ile BİREBİR AYNI gövde, aynı yanıt, aynı atlama kuralları.
        Audit action öneki `blog_post.bulk_<action>`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BulkContentActionRequest" }
      responses:
        "200":
          description: İşlem uygulandı (kısmi başarı dahil)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/BulkContentActionResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/pages/{pageId}/revisions:
    parameters:
      - $ref: "#/components/parameters/PageId"
    get:
      tags: [ContentRevisions]
      summary: Sayfanın revizyon geçmişini listele (ADMIN/EDITOR)
      description: >
        Liste görünümü `snapshot` alanını İÇERMEZ (id, editedById, editedByName,
        createdAt) — tam snapshot için `GET .../revisions/{revisionId}`. En yeni
        revizyon önce gelir.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ContentRevisionSummary" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/pages/{pageId}/revisions/{revisionId}:
    parameters:
      - $ref: "#/components/parameters/PageId"
      - $ref: "#/components/parameters/RevisionId"
    get:
      tags: [ContentRevisions]
      summary: Bir revizyonun tam snapshot'ını getir (ADMIN/EDITOR)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/ContentRevision" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/pages/{pageId}/revisions/{revisionId}/restore:
    parameters:
      - $ref: "#/components/parameters/PageId"
      - $ref: "#/components/parameters/RevisionId"
    post:
      tags: [ContentRevisions]
      summary: Sayfayı bu revizyondaki hale geri döndür (ADMIN/EDITOR)
      description: >
        Geri dönüş de geri alınabilsin diye önce mevcut state yeni bir revizyon
        olarak kaydedilir, ardından hedef revizyonun snapshot'ı uygulanır.
      responses:
        "200":
          description: Sayfa geri yüklendi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/Page" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/blog/{postId}/revisions:
    parameters:
      - $ref: "#/components/parameters/PostId"
    get:
      tags: [ContentRevisions]
      summary: Yazının revizyon geçmişini listele (ADMIN/EDITOR)
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ContentRevisionSummary" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/blog/{postId}/revisions/{revisionId}:
    parameters:
      - $ref: "#/components/parameters/PostId"
      - $ref: "#/components/parameters/RevisionId"
    get:
      tags: [ContentRevisions]
      summary: Bir revizyonun tam snapshot'ını getir (ADMIN/EDITOR)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/ContentRevision" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/blog/{postId}/revisions/{revisionId}/restore:
    parameters:
      - $ref: "#/components/parameters/PostId"
      - $ref: "#/components/parameters/RevisionId"
    post:
      tags: [ContentRevisions]
      summary: Yazıyı bu revizyondaki hale geri döndür (ADMIN/EDITOR)
      description: >
        Geri dönüş de geri alınabilsin diye önce mevcut state yeni bir revizyon
        olarak kaydedilir, ardından hedef revizyonun snapshot'ı uygulanır.
      responses:
        "200":
          description: Yazı geri yüklendi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/BlogPost" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/notifications/templates:
    get:
      tags: [EmailTemplates]
      summary: Tüm e-posta şablonlarını listele (yalnızca ADMIN)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EmailTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /admin/notifications/templates/{key}:
    parameters:
      - $ref: "#/components/parameters/TemplateKey"
    get:
      tags: [EmailTemplates]
      summary: Bir e-posta şablonunu getir (yalnızca ADMIN)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/EmailTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      tags: [EmailTemplates]
      summary: Bir e-posta şablonunu güncelle (yalnızca ADMIN)
      description: "Audit log: `notifications.template_update`."
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateEmailTemplateRequest" }
      responses:
        "200":
          description: Güncellendi
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/EmailTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/notifications/templates/{key}/preview:
    parameters:
      - $ref: "#/components/parameters/TemplateKey"
    post:
      tags: [EmailTemplates]
      summary: Şablonu örnek değerlerle render ederek önizle (yalnızca ADMIN, DB'ye yazmaz)
      description: >
        Değişken render'ı allow-list'e dayalı, güvenli string replace ile yapılır
        (`EmailTemplate.availableVariables`) — bir template engine/`eval` kullanılmaz.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PreviewEmailTemplateRequest" }
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/PreviewEmailTemplateResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/import/jobs:
    get:
      tags: [Import]
      summary: İçe aktarma işi geçmişini listele (yalnızca ADMIN)
      description: >
        Cursor sayfalı, `seq DESC` (en yeni önce) sıralı. `preview` alanı liste
        görünümünde DÖNMEZ (büyük olabilir) — detay için `GET /admin/import/jobs/{jobId}`.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/ImportJobTypeFilter"
        - $ref: "#/components/parameters/ImportJobStatusFilter"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ImportJobSummary" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [Import]
      summary: 1. ADIM — dosya yükle ve ÖNİZLEME üret (yalnızca ADMIN, İÇERİK YAZMAZ)
      description: >
        `multipart/form-data`. **Bu uç HİÇBİR Page/BlogPost/User/Media kaydı OLUŞTURMAZ.**
        Dosyayı gizli (public servis edilmeyen) bir konuma yazar, ayrıştırıp doğrular ve
        `status: PENDING` bir `ImportJob` üretir; `preview` alanı kaç kayıt bulunduğunu,
        hangi alanların eşleştiğini/eşleşmediğini ve ilk 5 örnek satırı taşır. Yazma
        2. adımda (`POST .../start`) başlar.


        **Alanlar:**

        - `file` (binary, ZORUNLU) — tek dosya.

        - `type` (string, ZORUNLU) — `#/components/schemas/ImportJobType`.


        `format` İSTEMCİDEN ALINMAZ, sunucu dosyanın gerçek içeriğinden (magic bytes +
        yapısal ayrıştırma) türetir; uzantı/`Content-Type` bilgi amaçlıdır, GÜVENİLMEZ.
        Geçerlilik matrisi (ihlal → 422): `PAGES`,`BLOG` → CSV|JSON · `WORDPRESS` → XML ·
        `USERS` → CSV · `MEDIA` → ZIP.


        **Boyut limitleri** (global 5 MB multipart limitinin route-level override'ı —
        `request.parts({ limits })` + route `bodyLimit`, bkz. ARCHITECTURE.md §10.8.5):
        `PAGES`/`BLOG`/`USERS` 10 MB · `WORDPRESS` 50 MB · `MEDIA` 100 MB.
        **Her boyut aşımı İSTİSNASIZ 413 `PAYLOAD_TOO_LARGE` döner** — tip-bazlı limit de,
        100 MB'lık sert üst tavan da, Fastify core `bodyLimit`i de. 422 DÖNMEZ (bkz.
        ARCHITECTURE.md §10.8.5 "413/422 kararı").


        **Kayıt sayısı tavanları** (aşım → 422, iş oluşturulmaz): `USERS` 500 satır ·
        `PAGES`/`BLOG` 5.000 satır · `WORDPRESS` 10.000 item · `MEDIA` 500 dosya.


        **Hız sınırı:** 10 istek / 10 dakika (route-level). Aşım → 429.


        Audit log: `import.upload` (metadata: `type`, `format`, `sizeBytes`, `totalCount`).
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema: { $ref: "#/components/schemas/CreateImportJobRequest" }
      responses:
        "201":
          description: "Dosya kabul edildi, önizleme hazır (`status: PENDING`)"
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/ImportJob" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "413": { $ref: "#/components/responses/PayloadTooLarge" }
        "422":
          description: >
            Dosya yok / bozuk / `type` ile format uyuşmuyor / kayıt tavanı aşıldı /
            XML'de DOCTYPE-DTD tespit edildi veya iç içe geçme 100 seviyeyi aştı (bkz.
            ARCHITECTURE.md §10.8.6 XXE savunması) / ZIP bombası ya da güvensiz arşiv
            yolu (zip-slip) tespit edildi. `error.details` alan-bazlı açıklama taşır.


            **Bu kontrollerin HEPSİ yükleme anında yapılır ve iş OLUŞTURULMAZ** — kaynak
            dosyadan statik olarak tespit edilebilen hiçbir kusur `FAILED` bir işe
            dönüşmez. `FAILED`, yalnızca çalışma anında ortaya çıkan hatalara ayrılmıştır
            (DB hatası, sunucu yeniden başlatma). Boyut aşımı bu daldan DEĞİL, 413'ten döner.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }
        "429": { $ref: "#/components/responses/TooManyRequests" }

  /admin/import/jobs/{jobId}:
    parameters:
      - $ref: "#/components/parameters/ImportJobId"
    get:
      tags: [Import]
      summary: İşin durumunu/ilerlemesini/özet raporunu getir — POLL UCU (yalnızca ADMIN)
      description: >
        Frontend bu ucu **2 saniyede bir** poll eder (WebSocket/SSE YOKTUR — projede
        realtime altyapısı yok, bkz. ARCHITECTURE.md §10.8). Worker sayaçları her 25
        kayıtta bir DB'ye yazar, dolayısıyla ilerleme çubuğu adım adım artar.


        `status` ∈ `COMPLETED|FAILED|CANCELLED` olduğunda poll durdurulur ve özet rapor
        gösterilir: "`totalCount` kayıttan `successCount` başarılı, `errorCount` hata"
        (+ `skippedCount`). Hata nedenleri için `GET .../errors`.


        Kullanıcı sayfadan ayrılabilir: iş sunucu tarafında devam eder, dönüşte bu uç
        güncel durumu verir. Bildirim, frontend'in bu ucu poll etmesiyle üretilen bir
        toast'tır (bkz. ARCHITECTURE.md §10.6 toast standardı).
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/ImportJob" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [Import]
      summary: İş kaydını sil — geçmişten kaldır (yalnızca ADMIN)
      description: >
        `ImportJobError` satırlarını (FK cascade) ve varsa kalan kaynak dosyayı da siler.
        **İçe aktarılmış içeriği GERİ ALMAZ** — geri alma (rollback) v1 KAPSAM DIŞIDIR;
        yanlış içe aktarım, ilgili listelerden toplu çöpe taşıma ile temizlenir
        (`POST /admin/{pages|blog}/bulk`, `action: trash`).


        `status: PROCESSING` olan iş SİLİNEMEZ → 409 (`"İşlenen bir iş silinemez. Önce
        iptal edin."`). Audit log: `import.delete`.
      responses:
        "204": { description: Silindi }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "İş işlenirken silinemez (`error.code: CONFLICT`)"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }

  /admin/import/jobs/{jobId}/start:
    parameters:
      - $ref: "#/components/parameters/ImportJobId"
    post:
      tags: [Import]
      summary: 2. ADIM — onayla ve arka planda işlemeyi başlat (yalnızca ADMIN)
      description: >
        `PENDING` → `QUEUED` geçişi yapar ve **202 ile HEMEN döner**; yazma işlemi
        istek/yanıt döngüsünün DIŞINDA, aynı Node süreci içinde asenkron olarak yürür
        (kuyruk altyapısı yok — karar ve gerekçesi ARCHITECTURE.md §10.8'de).


        Aynı anda **yalnızca BİR iş** `PROCESSING` olabilir; ikinci iş `QUEUED`'da
        sırasını bekler. Bu, isteği başarısız KILMAZ.


        `status` `PENDING` değilse → 409 (`"Bu iş zaten başlatılmış."`) — istek
        idempotent DEĞİLDİR, çift tıklama korumasını frontend + bu 409 birlikte sağlar.


        Gövde, önizleme ekranında yapılan seçimleri taşır (alan eşleştirme + çakışma
        stratejisi). Gövde boş gönderilirse `POST /admin/import/jobs` yanıtındaki
        `preview.suggestedMapping` ve `duplicateStrategy: skip` varsayılanları uygulanır.


        Audit log: `import.start` (metadata: `type`, `totalCount`, `duplicateStrategy`).
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StartImportJobRequest" }
      responses:
        "202":
          description: "Kabul edildi, arka planda işleniyor (`status: QUEUED`)"
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/ImportJob" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "İş `PENDING` durumunda değil (`error.code: CONFLICT`)"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }
        "422": { $ref: "#/components/responses/ValidationError" }

  /admin/import/jobs/{jobId}/cancel:
    parameters:
      - $ref: "#/components/parameters/ImportJobId"
    post:
      tags: [Import]
      summary: İşi iptal et (yalnızca ADMIN)
      description: >
        **İşbirlikçi (cooperative) iptaldir:** `cancelRequestedAt = now()` yazılır, worker
        bunu HER PARTİ (25 kayıt) başında kontrol eder ve fark ettiğinde durup
        `status: CANCELLED` yapar. Bu yüzden yanıt anında `status` hâlâ `PROCESSING`
        olabilir — frontend poll ederek `CANCELLED`'ı bekler.


        **O ana kadar yazılmış kayıtlar GERİ ALINMAZ** (rollback yok, bkz.
        `DELETE /admin/import/jobs/{jobId}`); sayaçlar nerede kalındığını gösterir.
        `PENDING`/`QUEUED` bir iş anında `CANCELLED` olur (hiç kayıt yazılmamıştır).


        `COMPLETED|FAILED|CANCELLED` bir iş için → 409. Audit log: `import.cancel`.
      responses:
        "200":
          description: İptal talebi alındı
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/ImportJob" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "İş zaten sonlanmış (`error.code: CONFLICT`)"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }

  /admin/import/jobs/{jobId}/errors:
    parameters:
      - $ref: "#/components/parameters/ImportJobId"
    get:
      tags: [Import]
      summary: İşin satır-bazlı hata/atlama kayıtlarını listele (yalnızca ADMIN)
      description: >
        Özet rapordaki "3 hata" satırına tıklanınca açılan liste. Cursor sayfalı,
        `seq ASC` (dosyadaki sırayla). İş başına EN FAZLA 1.000 hata satırı saklanır;
        aşıldığında yazma durur (`ImportJob.errorCount` gerçek sayıyı saymaya devam eder)
        ve `meta.truncated: true` döner.


        Bu satırlar `rawData` içinde KİŞİSEL VERİ taşıyabilir (ör. `USERS` içe aktarımında
        e-posta) → saklama süresi ve maskeleme kuralı compliance-agent'ın alanıdır
        (bkz. ARCHITECTURE.md §10.8 "Saklama & KVKK").
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ImportJobError" }
                      meta: { $ref: "#/components/schemas/ImportJobErrorListMeta" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/reports/exports:
    get:
      tags: [Reports]
      summary: Dışa aktarma işi geçmişini listele (yalnızca SiteRole=ADMIN)
      description: >
        Cursor sayfalı, `seq DESC` (en yeni önce). `type`/`status` ile filtrelenebilir.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/ExportJobTypeFilter"
        - $ref: "#/components/parameters/ExportJobStatusFilter"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ExportJob" }
                      meta: { $ref: "#/components/schemas/ExportJobListMeta" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [Reports]
      summary: Yeni bir analitik rapor dışa aktarma işi oluştur (yalnızca SiteRole=ADMIN)
      description: >
        **202 ile HEMEN döner** (`status: PENDING`); rapor (CSV/PDF) süreç-içi bir worker
        tarafından arka planda üretilir — kuyruk altyapısı YOK (`/admin/import/jobs`'un
        2. adımıyla AYNI desen, bkz. ARCHITECTURE.md §10.8.1/§10.8.10). Hazır olduğunda
        `GET /{jobId}` ile durum poll edilir, `status: COMPLETED` olunca
        `GET /{jobId}/download` ile indirilir.


        `from`/`to` ZORUNLUDUR (aralık en fazla 366 gün — aşımı 422, iş HİÇ
        oluşturulmaz). `filters` yalnızca ilgili `type` için anlamlıdır: `USERS` →
        `filters.role`, `REVENUE` → `filters.subscriptionStatus`; diğer tiplerde YOK
        SAYILIR.


        **`unmaskPii`** (varsayılan `false`) — `true` verilirse `USERS`/`REVENUE`
        raporlarındaki e-posta gibi alanlar MASKELENMEDEN dönülür. Bu zaten ADMIN-only
        bir uç olduğu için AYRI bir onay akışı YOKTUR, ancak `reports.export.unmasked_pii`
        adıyla AYRI bir audit kaydı yazılır ve üretilen `ExportJob.containsPii = true`
        set edilir. Saklama süresi bu durumda KISALIR: varsayılan (maskeli) dosyalar 7
        gün, `unmaskPii: true` ile üretilenler yalnızca 48 saat saklanır (compliance-agent
        kararı, bkz. ARCHITECTURE.md §10.8.10).


        Audit log: `reports.export.create` (+ `unmaskPii: true` ise ayrıca
        `reports.export.unmasked_pii`).


        **Hız sınırı:** 10 istek / 10 dakika (route-level, pahalı DB aggregation +
        PDF/CSV üretimi tetiklediği için). Aşım → 429.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateExportJobRequest" }
      responses:
        "202":
          description: "Kabul edildi, arka planda üretiliyor (`status: PENDING`)"
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/ExportJob" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationError" }
        "429": { $ref: "#/components/responses/TooManyRequests" }

  /admin/reports/exports/{jobId}:
    parameters:
      - $ref: "#/components/parameters/ExportJobId"
    get:
      tags: [Reports]
      summary: İşin durumunu/sonucunu getir — POLL UCU (yalnızca SiteRole=ADMIN)
      description: >
        `status` `COMPLETED` olduğunda `GET .../download` ile dosya indirilebilir hâle
        gelir. `status` `FAILED` ise `errorSummary` başarısızlık nedenini taşır.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/ApiSuccessEnvelope"
                  - properties:
                      data: { $ref: "#/components/schemas/ExportJob" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/reports/exports/{jobId}/download:
    parameters:
      - $ref: "#/components/parameters/ExportJobId"
    get:
      tags: [Reports]
      summary: Hazır dosyayı indir (yalnızca SiteRole=ADMIN)
      description: >
        `Content-Type` rapor formatına göre `text/csv; charset=utf-8` ya da
        `application/pdf`'tir; `Content-Disposition: attachment` ile birlikte döner
        (dosya adı `export-{type}-{jobId}.{csv|pdf}`). `storagePath` API'de ASLA
        dönmez — dosya bu uç üzerinden akıtılır.


        `status !== COMPLETED` (dosya henüz hazır değil) → 409. `expiresAt` geçmişse
        (saklama süresi dolmuşsa) → 404, `status` ne olursa olsun. Audit log:
        `reports.export.download` (metadata: `containsPii`).
      responses:
        "200":
          description: Dosya akışı
          content:
            text/csv:
              schema: { type: string, format: binary }
            application/pdf:
              schema: { type: string, format: binary }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: "İş bulunamadı VEYA indirme süresi dolmuş (`error.code: NOT_FOUND`)"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }
        "409":
          description: "Rapor henüz hazır değil (`error.code: CONFLICT`)"
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }

  /webhooks/stripe:
    post:
      tags: [Webhooks]
      security: []
      summary: Stripe webhook alıcı (imza `Stripe-Signature` header ile doğrulanır)
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        "200": { description: İşlendi }
        "400": { description: Geçersiz imza }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  parameters:
    OrgId:
      name: orgId
      in: path
      required: true
      schema: { type: string, format: uuid }
    Cursor:
      name: cursor
      in: query
      required: false
      schema: { type: string }
    Limit:
      name: limit
      in: query
      required: false
      schema: { type: integer, default: 20, maximum: 100 }
    PageId:
      name: pageId
      in: path
      required: true
      schema: { type: string, format: uuid }
    PostId:
      name: postId
      in: path
      required: true
      schema: { type: string, format: uuid }
    RevisionId:
      name: revisionId
      in: path
      required: true
      schema: { type: string, format: uuid }
    TrashedFilter:
      name: trashed
      in: query
      required: false
      description: >
        Çöp kutusu filtresi (§10.7). `exclude` (varsayılan) = yalnızca
        `deletedAt IS NULL`; `include` = çöptekiler dahil tümü; `only` = yalnızca çöp.
      schema:
        type: string
        enum: [exclude, include, only]
        default: exclude
    ContentStatusFilter:
      name: status
      in: query
      required: false
      description: >
        Sunucu tarafı durum filtresi. v1 admin liste ekranı KULLANMAZ (sekmeler
        client-side filtreler) — API tüketicileri ve ileride sunucu-taraflı sayfalama için.
      schema: { type: string, enum: [DRAFT, PUBLISHED] }
    TemplateKey:
      name: key
      in: path
      required: true
      description: "ör. WELCOME, PASSWORD_RESET, SYSTEM_ANNOUNCEMENT"
      schema: { type: string }
    ImportJobId:
      name: jobId
      in: path
      required: true
      schema: { type: string, format: uuid }
    ImportJobTypeFilter:
      name: type
      in: query
      required: false
      schema: { $ref: "#/components/schemas/ImportJobType" }
    ImportJobStatusFilter:
      name: status
      in: query
      required: false
      schema: { $ref: "#/components/schemas/ImportJobStatus" }
    StatsDays:
      name: days
      in: query
      required: false
      deprecated: true
      description: >
        DEPRECATED — `from`/`to` kullanın. `from`/`to` verilmezse geriye uyumluluk
        amacıyla kullanılır (varsayılan son 30 gün, en fazla 90 gün). `from`/`to` ile
        birlikte verilirse `from`/`to` kazanır.
      schema: { type: integer, minimum: 1, maximum: 90 }
    StatsFrom:
      name: from
      in: query
      required: false
      description: "ISO-8601 (UTC) başlangıç tarihi. `to` ile BİRLİKTE verilmelidir."
      schema: { type: string, format: date-time }
    StatsTo:
      name: to
      in: query
      required: false
      description: >
        ISO-8601 (UTC) bitiş tarihi. `from` ile BİRLİKTE verilmelidir. `from`/`to`
        aralığı en fazla 366 gün olabilir ve `from` <= `to` olmalıdır — aşımı 422
        (`ValidationError`) ile reddedilir, iş/istek oluşturulmaz.
      schema: { type: string, format: date-time }
    StatsGranularity:
      name: granularity
      in: query
      required: false
      description: "`PageView`/kullanıcı/gelir günlük bucket'ının haftalık/aylık'a toplanması. Varsayılan `day`."
      schema:
        allOf:
          - $ref: "#/components/schemas/Granularity"
        default: day
    StatsCompare:
      name: compare
      in: query
      required: false
      description: >
        `true` ise bir önceki eş-uzunluktaki dönemle karşılaştırma deltası döner.
        Yalnızca `GET /admin/stats/summary` tarafından KULLANILIR — diğer `/admin/stats/*`
        uçları bu parametreyi kabul eder ama YOK SAYAR.
      schema: { type: boolean, default: false }
    ExportJobId:
      name: jobId
      in: path
      required: true
      schema: { type: string, format: uuid }
    ExportJobTypeFilter:
      name: type
      in: query
      required: false
      schema: { $ref: "#/components/schemas/ExportJobType" }
    ExportJobStatusFilter:
      name: status
      in: query
      required: false
      schema: { $ref: "#/components/schemas/ExportJobStatus" }

  responses:
    Unauthorized:
      description: Kimlik doğrulama yok/geçersiz
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }
    Forbidden:
      description: Yetki yok
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }
    NotFound:
      description: Kaynak bulunamadı
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }
    Conflict:
      description: Çakışma (ör. e-posta zaten kayıtlı)
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }
    ValidationError:
      description: Girdi doğrulama hatası
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }
    TooManyRequests:
      description: "Hız sınırı aşıldı (`error.code: RATE_LIMITED`)"
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }
    PayloadTooLarge:
      description: >
        Yüklenen dosya/istek gövdesi izin verilen boyutu aşıyor
        (`error.code: PAYLOAD_TOO_LARGE`, bkz. lib/errors.ts::PayloadTooLargeError).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiErrorEnvelope" }

  schemas:
    ApiSuccessEnvelope:
      type: object
      required: [data]
      properties:
        data: {}
        meta:
          type: object
          properties:
            nextCursor: { type: string, nullable: true }

    ApiErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              enum:
                [
                  UNAUTHORIZED,
                  FORBIDDEN,
                  NOT_FOUND,
                  VALIDATION_ERROR,
                  CONFLICT,
                  RATE_LIMITED,
                  PAYLOAD_TOO_LARGE,
                  INTERNAL_ERROR,
                ]
            message: { type: string }
            details:
              type: object
              additionalProperties:
                type: array
                items: { type: string }

    User:
      type: object
      properties:
        id: { type: string, format: uuid }
        email: { type: string, format: email }
        name: { type: string }
        avatarUrl: { type: string, nullable: true }
        emailVerifiedAt: { type: string, format: date-time, nullable: true }
        role: { type: string, enum: [ADMIN, EDITOR, VIEWER] }
        createdAt: { type: string, format: date-time }

    AdminUser:
      description: "`/admin/users` uçlarında dönen genişletilmiş kullanıcı DTO'su — yalnızca ADMIN görebilir."
      allOf:
        - $ref: "#/components/schemas/User"
        - type: object
          properties:
            status: { type: string, enum: [ACTIVE, SUSPENDED] }
            lastLoginAt: { type: string, format: date-time, nullable: true }

    AuditLog:
      type: object
      properties:
        id: { type: string, format: uuid }
        actorId: { type: string, format: uuid, nullable: true }
        actorEmail: { type: string, nullable: true }
        action: { type: string, example: "user.role_change" }
        status: { type: string, enum: [SUCCESS, FAILURE, FORBIDDEN] }
        targetType: { type: string, nullable: true }
        targetId: { type: string, nullable: true }
        metadata:
          type: object
          nullable: true
          additionalProperties: true
        ipAddress: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }

    CreateAdminUserRequest:
      type: object
      required: [name, email]
      properties:
        name: { type: string, minLength: 1 }
        email: { type: string, format: email }
        role: { type: string, enum: [ADMIN, EDITOR, VIEWER], default: EDITOR }

    CreateAdminUserResponse:
      type: object
      properties:
        user: { $ref: "#/components/schemas/AdminUser" }
        setPasswordUrl: { type: string, format: uri }

    UpdateAdminUserRoleRequest:
      type: object
      required: [role]
      properties:
        role: { type: string, enum: [ADMIN, EDITOR, VIEWER] }

    UpdateAdminUserStatusRequest:
      type: object
      required: [status]
      properties:
        status: { type: string, enum: [ACTIVE, SUSPENDED] }

    PermissionsMatrix:
      type: object
      properties:
        roles:
          type: array
          items: { type: string, enum: [ADMIN, EDITOR, VIEWER] }
        modules:
          type: array
          items:
            type: object
            properties:
              module: { type: string }
              label: { type: string }
              actions:
                type: object
                additionalProperties:
                  type: array
                  items: { type: string, enum: [ADMIN, EDITOR, VIEWER] }

    Organization:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        slug: { type: string }
        ownerId: { type: string, format: uuid }
        createdAt: { type: string, format: date-time }

    Membership:
      type: object
      properties:
        id: { type: string, format: uuid }
        userId: { type: string, format: uuid }
        organizationId: { type: string, format: uuid }
        role: { type: string, enum: [OWNER, ADMIN, MEMBER] }
        status: { type: string, enum: [ACTIVE, INVITED, SUSPENDED] }
        user:
          type: object
          properties:
            id: { type: string, format: uuid }
            name: { type: string }
            email: { type: string, format: email }
            avatarUrl: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }

    Invitation:
      type: object
      properties:
        id: { type: string, format: uuid }
        organizationId: { type: string, format: uuid }
        email: { type: string, format: email }
        role: { type: string, enum: [ADMIN, MEMBER] }
        status: { type: string, enum: [PENDING, ACCEPTED, EXPIRED, REVOKED] }
        expiresAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }

    Plan:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        priceMonthlyCents: { type: integer }
        priceYearlyCents: { type: integer }
        currency: { type: string, example: TRY }
        limits:
          type: object
          additionalProperties: { type: integer }

    Subscription:
      type: object
      properties:
        id: { type: string, format: uuid }
        organizationId: { type: string, format: uuid }
        plan: { $ref: "#/components/schemas/Plan" }
        status: { type: string, enum: [TRIALING, ACTIVE, PAST_DUE, CANCELED, INCOMPLETE] }
        currentPeriodEnd: { type: string, format: date-time }
        cancelAtPeriodEnd: { type: boolean }

    AuthTokens:
      type: object
      properties:
        accessToken: { type: string }
        accessTokenExpiresAt: { type: string, format: date-time }

    AuthSession:
      type: object
      properties:
        user: { $ref: "#/components/schemas/User" }
        memberships:
          type: array
          items:
            type: object
            properties:
              organizationId: { type: string, format: uuid }
              role: { type: string, enum: [OWNER, ADMIN, MEMBER] }

    AuthResponse:
      type: object
      properties:
        user: { $ref: "#/components/schemas/User" }
        tokens: { $ref: "#/components/schemas/AuthTokens" }

    RegisterRequest:
      type: object
      required: [email, password, name]
      properties:
        email: { type: string, format: email }
        password: { type: string, minLength: 8 }
        name: { type: string, minLength: 1 }

    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email: { type: string, format: email }
        password: { type: string }

    UpdateUserRequest:
      description: >
        Kendi profilini güncelleme (`PATCH /users/me`). Yalnızca `name` ve `avatarUrl`
        değiştirilebilir — `email`, `role`, `status` BU UÇTAN DEĞİŞTİRİLEMEZ (rol/durum
        için bkz. `/admin/users/{userId}/role|status`). `avatarUrl: null` gönderilirse
        avatar kaldırılır (boş string `""` GEÇERSİZDİR, 422 döner).
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 80 }
        avatarUrl: { type: string, format: uri, nullable: true }

    ChangePasswordRequest:
      description: >
        `POST /users/me/change-password` gövdesi. `newPassword` kuralı `RegisterRequest`
        ve `/auth/reset-password` ile AYNIDIR (min 8 karakter) — tek kaynak: bu kontrat.
        `newPassword === currentPassword` ise 422 (`details.newPassword`) döner.
        Yeni şifre tekrarı (`confirm`) YALNIZCA istemci tarafı doğrulamasıdır, gövdede
        GÖNDERİLMEZ.
      type: object
      required: [currentPassword, newPassword]
      properties:
        currentPassword: { type: string, minLength: 1 }
        newPassword: { type: string, minLength: 8 }

    CreateOrganizationRequest:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1 }

    UpdateOrganizationRequest:
      type: object
      properties:
        name: { type: string, minLength: 1 }

    CreateInvitationRequest:
      type: object
      required: [email, role]
      properties:
        email: { type: string, format: email }
        role: { type: string, enum: [ADMIN, MEMBER] }

    UpdateMembershipRequest:
      type: object
      required: [role]
      properties:
        role: { type: string, enum: [ADMIN, MEMBER] }

    CreateCheckoutSessionRequest:
      type: object
      required: [planId, billingCycle, successUrl, cancelUrl]
      properties:
        planId: { type: string, format: uuid }
        billingCycle: { type: string, enum: [monthly, yearly] }
        successUrl: { type: string, format: uri }
        cancelUrl: { type: string, format: uri }

    CheckoutSessionResponse:
      type: object
      properties:
        checkoutUrl: { type: string, format: uri }

    BillingPortalResponse:
      type: object
      properties:
        portalUrl: { type: string, format: uri }

    NavigationItem:
      type: object
      properties:
        id: { type: string }
        label: { type: string }
        href: { type: string }
        order: { type: integer }

    SocialLink:
      type: object
      properties:
        id: { type: string }
        platform: { type: string, enum: [TWITTER, GITHUB, LINKEDIN, INSTAGRAM, FACEBOOK, YOUTUBE, OTHER] }
        url: { type: string }
        order: { type: integer }

    FooterLink:
      type: object
      properties:
        id: { type: string }
        label: { type: string }
        href: { type: string }
        order: { type: integer }

    FooterColumn:
      type: object
      properties:
        id: { type: string }
        title: { type: string }
        order: { type: integer }
        links:
          type: array
          items: { $ref: "#/components/schemas/FooterLink" }

    NavigationConfig:
      type: object
      properties:
        headerCtaLabel: { type: string, nullable: true }
        headerCtaHref: { type: string, nullable: true }
        footerCopyrightText: { type: string, nullable: true }
        navigationItems:
          type: array
          items: { $ref: "#/components/schemas/NavigationItem" }
        socialLinks:
          type: array
          items: { $ref: "#/components/schemas/SocialLink" }
        footerColumns:
          type: array
          items: { $ref: "#/components/schemas/FooterColumn" }

    UpdateNavigationConfigRequest:
      description: >
        Tam değiştirme (replace) semantiği — `navigationItems`/`socialLinks`/`footerColumns`
        dizileri gönderilen haliyle mevcut kayıtların tamamının yerini alır; `id` alanları
        istemciden gönderilmez (sunucu yeniden üretir).
      type: object
      required: [navigationItems, socialLinks, footerColumns]
      properties:
        headerCtaLabel: { type: string, maxLength: 60, nullable: true }
        headerCtaHref:
          type: string
          maxLength: 500
          nullable: true
          description: "yalnızca http(s)://, / veya # ile başlayan yollar kabul edilir"
        footerCopyrightText: { type: string, maxLength: 200, nullable: true }
        navigationItems:
          type: array
          maxItems: 20
          items:
            type: object
            required: [label, href, order]
            properties:
              label: { type: string, minLength: 1, maxLength: 80 }
              href: { type: string, minLength: 1, maxLength: 500 }
              order: { type: integer, minimum: 0 }
        socialLinks:
          type: array
          maxItems: 15
          items:
            type: object
            required: [platform, url, order]
            properties:
              platform: { type: string, enum: [TWITTER, GITHUB, LINKEDIN, INSTAGRAM, FACEBOOK, YOUTUBE, OTHER] }
              url: { type: string, minLength: 1, maxLength: 500 }
              order: { type: integer, minimum: 0 }
        footerColumns:
          type: array
          maxItems: 8
          items:
            type: object
            required: [title, order, links]
            properties:
              title: { type: string, minLength: 1, maxLength: 80 }
              order: { type: integer, minimum: 0 }
              links:
                type: array
                maxItems: 20
                items:
                  type: object
                  required: [label, href, order]
                  properties:
                    label: { type: string, minLength: 1, maxLength: 80 }
                    href: { type: string, minLength: 1, maxLength: 500 }
                    order: { type: integer, minimum: 0 }


    DailyViewStats:
      type: object
      properties:
        date: { type: string, format: date, example: "2026-07-30" }
        pageViews: { type: integer }
        postViews: { type: integer }

    LiveVisitors:
      type: object
      properties:
        count:
          type: integer
          description: >
            Son 60 saniye içinde bir görüntülenme endpoint'ini çağıran benzersiz
            IP+User-Agent sayısı (süreç-içi/in-memory, dağıtık değil).

    DeviceBreakdownEntry:
      type: object
      properties:
        type: { type: string, enum: [MOBILE, DESKTOP, TABLET, UNKNOWN] }
        count: { type: integer }

    CountryBreakdownEntry:
      type: object
      properties:
        country:
          type: string
          description: >
            ISO 3166-1 alpha-2 ülke kodu, bilinmiyorsa "UNKNOWN"; ilk 10 dışında
            kalan ülkelerin toplamı "OTHER" olarak tek satırda döner.
        count: { type: integer }

    ViewsBreakdown:
      type: object
      properties:
        devices:
          type: array
          items: { $ref: "#/components/schemas/DeviceBreakdownEntry" }
        countries:
          type: array
          items: { $ref: "#/components/schemas/CountryBreakdownEntry" }

    Granularity:
      type: string
      enum: [day, week, month]
      description: "`PageView`/kullanıcı/gelir günlük bucket'ının toplama seçeneği."

    TopContentItem:
      type: object
      required: [contentType, id, title, slug, views]
      properties:
        contentType: { type: string, enum: [page, post] }
        id: { type: string, format: uuid }
        title: { type: string }
        slug: { type: string }
        views: { type: integer }

    TopContentListMeta:
      type: object
      properties:
        nextCursor: { type: string, nullable: true }

    SummaryCompare:
      type: object
      description: "`GET /admin/stats/summary?compare=true` yanıtındaki karşılaştırma deltası."
      required: [pageViews, postViews, newUsers]
      properties:
        pageViews: { type: integer }
        postViews: { type: integer }
        newUsers: { type: integer }

    SummaryStats:
      type: object
      required: [from, to, granularity, pageViews, postViews, newUsers, activeSubscriptions, mrrCents, compare]
      properties:
        from: { type: string, format: date-time }
        to: { type: string, format: date-time }
        granularity: { $ref: "#/components/schemas/Granularity" }
        pageViews: { type: integer }
        postViews: { type: integer }
        newUsers: { type: integer }
        activeSubscriptions:
          type: integer
          description: "ANLIK (şimdiki zaman) durum — dönem sonuna göre DEĞİL."
        mrrCents:
          type: integer
          description: >
            ANLIK MRR, yalnızca `Plan.priceMonthlyCents` toplamı — `Subscription`
            faturalama periyodunu (aylık/yıllık) ayrı bir alanda TUTMADIĞI için yıllık
            abonelikler ay karşılığına ÇEVRİLMEDEN, olduğu gibi toplanır.
        compare:
          allOf:
            - $ref: "#/components/schemas/SummaryCompare"
          nullable: true
          description: >
            `compare=true` istendiyse dolu, aksi hâlde `null`. `activeSubscriptions`/
            `mrrCents` için delta BİLEREK YOKTUR — `Subscription` geçmiş durum geçmişi
            tutan bir tablo değil, yalnızca ANLIK durumu taşıyor; geçmiş bir dönem için
            "o an aktif olan abonelik sayısı" güvenilir biçimde yeniden inşa edilemiyor.

    UserSeriesPoint:
      type: object
      properties:
        date: { type: string, format: date }
        count: { type: integer }

    RoleDistributionEntry:
      type: object
      properties:
        role: { type: string, enum: [ADMIN, EDITOR, VIEWER] }
        count: { type: integer }

    UsersStats:
      type: object
      required: [from, to, granularity, series, roleDistribution]
      properties:
        from: { type: string, format: date-time }
        to: { type: string, format: date-time }
        granularity: { $ref: "#/components/schemas/Granularity" }
        series:
          type: array
          items: { $ref: "#/components/schemas/UserSeriesPoint" }
        roleDistribution:
          type: array
          description: "Dönemle SINIRLI DEĞİL — kullanıcı tablosunun ANLIK rol dağılımı."
          items: { $ref: "#/components/schemas/RoleDistributionEntry" }

    RevenueSeriesPoint:
      type: object
      properties:
        date: { type: string, format: date }
        newMrrCents:
          type: integer
          description: "Bucket içinde OLUŞTURULAN aboneliklerin `priceMonthlyCents` toplamı (\"yeni MRR\")."
        churnedCount:
          type: integer
          description: "Bucket içinde `CANCELED` durumuna geçen (yaklaşık — `updatedAt` referans alınır) abonelik sayısı."

    RevenueStats:
      type: object
      required: [from, to, granularity, activeSubscriptions, mrrCents, series]
      properties:
        from: { type: string, format: date-time }
        to: { type: string, format: date-time }
        granularity: { $ref: "#/components/schemas/Granularity" }
        activeSubscriptions: { type: integer, description: "ANLIK toplam — bkz. `SummaryStats.activeSubscriptions`." }
        mrrCents: { type: integer, description: "ANLIK toplam — bkz. `SummaryStats.mrrCents`." }
        series:
          type: array
          items: { $ref: "#/components/schemas/RevenueSeriesPoint" }

    SystemHealth:
      type: object
      description: Tüm alanlar gerçek zamanlı ölçülür — sabit/uydurma değer içermez.
      properties:
        dbPingMs: { type: number, description: "`SELECT 1` sorgusunun ölçülen süresi (ms)" }
        dbSizeBytes: { type: number, description: "`pg_database_size(current_database())`" }
        dbQuotaBytes:
          type: number
          nullable: true
          description: "DB_STORAGE_QUOTA_MB tanımlı değilse null"
        mediaStorageBytes: { type: number, description: "Media.sizeBytes toplamı" }
        mediaStorageQuotaBytes:
          type: number
          nullable: true
          description: "MEDIA_STORAGE_QUOTA_MB tanımlı değilse null"
        memoryUsedBytes: { type: number }
        memoryTotalBytes: { type: number }
        processMemoryBytes: { type: number, description: "process.memoryUsage().rss" }
        loadAverage:
          type: array
          items: { type: number }
          minItems: 3
          maxItems: 3
          description: "os.loadavg() — [1dk, 5dk, 15dk]; Windows'ta her zaman [0,0,0] döner (platform kısıtı)"
        platform: { type: string, example: "linux" }
        uptimeSeconds: { type: number }
        checkedAt: { type: string, format: date-time }

    BlogCategory:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        slug: { type: string }
        createdAt: { type: string, format: date-time }

    Page:
      description: >
        `/admin/pages` ve `/pages` uçlarında dönen sayfa DTO'su. `translations.EN`
        yalnızca override'ları taşır (bkz. ARCHITECTURE.md §10.5); `?locale=EN` ile
        okunan public GET'lerde bu alanlar zaten TR'ye alan-bazlı fallback edilmiş
        olarak gelir.
      type: object
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        slug: { type: string }
        status: { type: string, enum: [DRAFT, PUBLISHED] }
        blocks:
          type: array
          items: { type: object, additionalProperties: true }
        seoTitle: { type: string, nullable: true }
        seoDescription: { type: string, nullable: true }
        ogTitle: { type: string, nullable: true }
        ogImageUrl: { type: string, nullable: true }
        canonicalUrl: { type: string, nullable: true }
        noIndex: { type: boolean }
        translations:
          type: object
          description: "ör. { \"EN\": { \"title\": \"...\", \"blocks\": [...] } } — bkz. shared-types.ts::PageTranslations"
          additionalProperties:
            type: object
            additionalProperties: true
        publishedAt: { type: string, format: date-time, nullable: true }
        viewCount: { type: integer }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        # ---- §10.7 İçerik Yönetim Listesi ----
        deletedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Soft-delete zaman damgası. `null` = aktif, dolu = ÇÖPTE. Public uçlar
            (`/pages`, `/pages/{slug}`, view sayacı, `SiteSettings.homePage`) bu alanı
            `null` olarak FİLTRELEMEK ZORUNDADIR.
        authorId: { type: string, format: uuid, nullable: true }
        author:
          allOf:
            - $ref: "#/components/schemas/UserSummary"
          nullable: true
          description: >
            Yazar özeti. Mevcut kayıtlarda backfill YAPILMAZ → eski sayfalarda `null`
            gelir, UI "—" gösterir. `authorId` ile birlikte döner: form/dropdown
            `authorId`'ye bağlanır, tablo `author`'ı render eder.
        seoScore:
          type: integer
          minimum: 0
          maximum: 100
          description: >
            Backend'de HER OKUMADA hesaplanan basit SEO tamlık skoru (persist EDİLMEZ,
            ekstra DB sorgusu yapılmaz). 5 kriter × 20 puan — bkz.
            `#/components/schemas/SeoScoreIssue` ve ARCHITECTURE.md §10.7.
            Rozet renk eşiği (frontend): <50 kırmızı, 50–79 sarı, ≥80 yeşil.
        seoScoreIssues:
          type: array
          description: >
            Puan kaybettiren kriterler. Boş dizi = 100 puan. Rozete tıklanınca bu liste
            gösterilir. Sıra deterministiktir (kriter numarasına göre).
          items: { $ref: "#/components/schemas/SeoScoreIssue" }

    BlogPost:
      description: >
        `/admin/blog` ve `/blog` uçlarında dönen yazı DTO'su. `translations.EN` yalnızca
        override'ları taşır (bkz. ARCHITECTURE.md §10.5).
      type: object
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        slug: { type: string }
        excerpt: { type: string, nullable: true }
        contentHtml: { type: string }
        coverImageUrl: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, PUBLISHED] }
        category:
          allOf:
            - $ref: "#/components/schemas/BlogCategory"
          nullable: true
        seoTitle: { type: string, nullable: true }
        seoDescription: { type: string, nullable: true }
        ogTitle: { type: string, nullable: true }
        ogImageUrl: { type: string, nullable: true }
        canonicalUrl: { type: string, nullable: true }
        noIndex: { type: boolean }
        translations:
          type: object
          description: "ör. { \"EN\": { \"title\": \"...\", \"contentHtml\": \"...\" } } — bkz. shared-types.ts::BlogPostTranslations"
          additionalProperties:
            type: object
            additionalProperties: true
        publishedAt: { type: string, format: date-time, nullable: true }
        viewCount: { type: integer }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        # ---- §10.7 İçerik Yönetim Listesi ----
        deletedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Soft-delete zaman damgası. Public uçlar (`/blog`, `/blog/{slug}`, view
            sayacı) `deletedAt: null` FİLTRESİ UYGULAMAK ZORUNDADIR.
        authorId: { type: string, format: uuid, nullable: true }
        author:
          allOf:
            - $ref: "#/components/schemas/UserSummary"
          nullable: true
          description: >
            **Şema zaten vardı (`BlogPost.authorId` + `author` ilişkisi), DTO'da EKSİKTİ**
            — bu sürümle DTO'ya ve mapper'a eklenir.
        seoScore:
          type: integer
          minimum: 0
          maximum: 100
          description: "Bkz. `Page.seoScore` — aynı algoritma, entity'ye özgü girdilerle."
        seoScoreIssues:
          type: array
          items: { $ref: "#/components/schemas/SeoScoreIssue" }

    UserSummary:
      description: >
        Kullanıcının listelerde gösterilen minimum özeti — hassas alan (rol/durum/
        e-posta doğrulaması) TAŞIMAZ. Yazar sütunu ve yazar dropdown'ı bunu kullanır.
      type: object
      required: [id, name, email]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        email: { type: string, format: email }
        avatarUrl: { type: string, nullable: true }

    ContentCounts:
      description: >
        Sekme sayaçları — tablo genelinde hesaplanır, istek filtrelerinden ETKİLENMEZ.
        WordPress semantiği: "Tümü" çöptekileri İÇERMEZ.
      type: object
      required: [all, published, draft, trashed]
      properties:
        all: { type: integer, description: "deletedAt IS NULL olan tüm kayıtlar (= published + draft)" }
        published: { type: integer, description: "status = PUBLISHED AND deletedAt IS NULL" }
        draft: { type: integer, description: "status = DRAFT AND deletedAt IS NULL" }
        trashed: { type: integer, description: "deletedAt IS NOT NULL" }

    ContentListMeta:
      description: "`GET /admin/pages` ve `GET /admin/blog` yanıtlarının `meta` zarfı."
      type: object
      required: [counts]
      properties:
        nextCursor: { type: string, nullable: true }
        counts: { $ref: "#/components/schemas/ContentCounts" }

    SeoScoreIssueCode:
      type: string
      description: >
        Kararlı makine-okunur kod. Frontend/qa mantığı BU KODA bağlanır; `label`
        yalnızca gösterim içindir (metni değişebilir).
      enum:
        - SEO_TITLE_MISSING
        - SEO_TITLE_LENGTH
        - SEO_DESCRIPTION_MISSING
        - SEO_DESCRIPTION_LENGTH
        - COVER_IMAGE_MISSING
        - IMAGE_MISSING
        - IMAGE_ALT_MISSING
        - CONTENT_TOO_SHORT

    SeoScoreIssue:
      description: >
        **Puanlama (toplam 100, 5 kriter × 20p):**


        1. Meta başlık: yok → 0p `SEO_TITLE_MISSING`; var ama uzunluk 50–60 dışında →
        10p `SEO_TITLE_LENGTH`; 50–60 arası → 20p.

        2. Meta açıklama: yok → 0p `SEO_DESCRIPTION_MISSING`; var ama 120–160 dışında →
        10p `SEO_DESCRIPTION_LENGTH`; 120–160 arası → 20p.

        3. Kapak görseli: yoksa 0p `COVER_IMAGE_MISSING`, varsa 20p.
        BlogPost → `coverImageUrl`, boşsa `ogImageUrl`.
        Page → `ogImageUrl`, boşsa ilk `image` bloğunun `data.url` değeri.

        4. Alt-metinli görsel: hiç görsel yoksa 0p `IMAGE_MISSING`; görsel var ama
        hiçbirinde dolu alt yoksa 0p `IMAGE_ALT_MISSING`; en az biri alt'lıysa 20p.
        Page → `blocks` içindeki `image`/`gallery` bloklarının `data.alt` (gallery'de
        herhangi bir öğenin alt'ı). BlogPost → `contentHtml` içindeki `<img alt="...">`;
        kapak görseli bu kriteri KARŞILAMAZ (3. kriterle çift sayılmasın diye).

        5. İçerik uzunluğu: kelime sayısı eşiğin altındaysa 0p `CONTENT_TOO_SHORT`,
        değilse 20p. Eşik: **Page ≥ 100**, **BlogPost ≥ 300** kelime. Kelime =
        HTML/etiketler ayıklandıktan sonra boşlukla ayrılmış boş olmayan token.
        Page metni: blokların `data.text`/`data.title`/`data.subtitle`/`data.html`
        alanlarının birleşimi. BlogPost metni: `contentHtml`.


        Skor yalnızca TR/kanonik kolonlardan hesaplanır (`translations` v1'de dikkate
        alınmaz). Hesap saf ve senkron olmalıdır — liste yanıtında satır başına çalışır.
      type: object
      required: [code, label]
      properties:
        code: { $ref: "#/components/schemas/SeoScoreIssueCode" }
        label:
          type: string
          description: "Kullanıcıya gösterilecek Türkçe metin, ör. \"Meta açıklama 120–160 karakter olmalı (şu an 84).\""

    BulkContentActionRequest:
      type: object
      required: [ids, action]
      properties:
        ids:
          type: array
          minItems: 1
          maxItems: 100
          items: { type: string, format: uuid }
          description: "Tekrarlı id'ler sunucuda teklenir (dedupe)."
        action:
          type: string
          enum: [trash, restore, publish, draft, permanent-delete]
          description: >
            `trash` = çöpe taşı; `restore` = geri yükle; `publish`/`draft` = durum
            değiştir (çöptekiler atlanır); `permanent-delete` = kalıcı sil
            (yalnızca ADMIN, yalnızca çöptekiler).

    BulkContentActionResult:
      type: object
      required: [action, requestedCount, affectedCount, skippedIds]
      properties:
        action: { type: string, enum: [trash, restore, publish, draft, permanent-delete] }
        requestedCount: { type: integer, description: "dedupe sonrası gönderilen id sayısı" }
        affectedCount: { type: integer }
        skippedIds:
          type: array
          items: { type: string, format: uuid }
          description: "Uygulanamayan id'ler (bulunamayan veya durumu uygun olmayan). Hata DEĞİLDİR."

    CreatePageRequest:
      type: object
      required: [title]
      properties:
        title: { type: string, minLength: 1 }
        slug: { type: string, minLength: 1, description: "verilmezse title'dan slugify edilir" }
        status: { type: string, enum: [DRAFT, PUBLISHED], default: DRAFT }
        blocks:
          type: array
          items: { type: object, additionalProperties: true }
        seoTitle: { type: string }
        seoDescription: { type: string }
        ogTitle: { type: string, nullable: true }
        ogImageUrl: { type: string, nullable: true }
        canonicalUrl: { type: string, format: uri, nullable: true }
        noIndex: { type: boolean, default: false }
        translations:
          type: object
          additionalProperties: { type: object, additionalProperties: true }
        authorId:
          type: string
          format: uuid
          nullable: true
          description: >
            §10.7 — verilmezse giriş yapmış kullanıcı atanır. Giriş yapmış kullanıcıdan
            FARKLI bir id yalnızca ADMIN tarafından gönderilebilir (EDITOR → 403);
            var olmayan kullanıcı → 422.

    UpdatePageRequest:
      description: >
        Tüm alanlar opsiyoneldir. Hızlı Düzenle (Quick Edit) bu şemanın
        `{ title, slug, status }` alt kümesini gönderir — ayrı bir uç/şema YOKTUR.
      type: object
      properties:
        title: { type: string, minLength: 1 }
        slug: { type: string, minLength: 1 }
        status: { type: string, enum: [DRAFT, PUBLISHED] }
        blocks:
          type: array
          items: { type: object, additionalProperties: true }
        seoTitle: { type: string, nullable: true }
        seoDescription: { type: string, nullable: true }
        ogTitle: { type: string, nullable: true }
        ogImageUrl: { type: string, nullable: true }
        canonicalUrl: { type: string, format: uri, nullable: true }
        noIndex: { type: boolean }
        translations:
          type: object
          additionalProperties: { type: object, additionalProperties: true }
        authorId:
          type: string
          format: uuid
          nullable: true
          description: "Yalnızca ADMIN değiştirebilir (EDITOR gönderirse 403). `null` = yazarı kaldır."

    CreateBlogPostRequest:
      type: object
      required: [title]
      properties:
        title: { type: string, minLength: 1 }
        slug: { type: string, minLength: 1 }
        excerpt: { type: string }
        contentHtml: { type: string }
        coverImageUrl: { type: string }
        status: { type: string, enum: [DRAFT, PUBLISHED], default: DRAFT }
        categoryId: { type: string, format: uuid, nullable: true }
        seoTitle: { type: string, nullable: true }
        seoDescription: { type: string, nullable: true }
        ogTitle: { type: string, nullable: true }
        ogImageUrl: { type: string, nullable: true }
        canonicalUrl: { type: string, format: uri, nullable: true }
        noIndex: { type: boolean, default: false }
        translations:
          type: object
          additionalProperties: { type: object, additionalProperties: true }
        authorId:
          type: string
          format: uuid
          nullable: true
          description: >
            §10.7 — `CreatePageRequest.authorId` ile aynı kural. **Mevcut kod bunu
            gövdeden okumadan `request.user.id` olarak sabitliyor**; yeni kuralda
            gövdeden gelen değer (ADMIN ise) önceliklidir.

    UpdateBlogPostRequest:
      description: "Tüm alanlar opsiyonel; Hızlı Düzenle `{ title, slug, status }` alt kümesini gönderir."
      type: object
      properties:
        title: { type: string, minLength: 1 }
        slug: { type: string, minLength: 1 }
        excerpt: { type: string, nullable: true }
        contentHtml: { type: string }
        coverImageUrl: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, PUBLISHED] }
        categoryId: { type: string, format: uuid, nullable: true }
        seoTitle: { type: string, nullable: true }
        seoDescription: { type: string, nullable: true }
        ogTitle: { type: string, nullable: true }
        ogImageUrl: { type: string, nullable: true }
        canonicalUrl: { type: string, format: uri, nullable: true }
        noIndex: { type: boolean }
        translations:
          type: object
          additionalProperties: { type: object, additionalProperties: true }
        authorId:
          type: string
          format: uuid
          nullable: true
          description: "Yalnızca ADMIN değiştirebilir."

    ContentEntityType:
      type: string
      enum: [PAGE, BLOG_POST]

    ContentRevisionSummary:
      description: >
        Revizyon liste görünümü — `snapshot` alanını İÇERMEZ (bkz. ARCHITECTURE.md §10.1).
      type: object
      properties:
        id: { type: string, format: uuid }
        editedById: { type: string, format: uuid, nullable: true }
        editedByName: { type: string, description: "silinen kullanıcı için okunabilir ad snapshot'ı" }
        createdAt: { type: string, format: date-time }

    ContentRevision:
      allOf:
        - $ref: "#/components/schemas/ContentRevisionSummary"
        - type: object
          properties:
            entityType: { $ref: "#/components/schemas/ContentEntityType" }
            entityId: { type: string }
            snapshot:
              type: object
              description: >
                Güncellemeden HEMEN ÖNCEKİ tam alan seti — Page için `{ title, slug,
                blocks, seoTitle, seoDescription, ogTitle, ogImageUrl, canonicalUrl,
                noIndex, translations }`; BlogPost için `{ title, slug, excerpt,
                contentHtml, coverImageUrl, categoryId, seoTitle, seoDescription,
                ogTitle, ogImageUrl, canonicalUrl, noIndex, translations }`.
              additionalProperties: true

    EmailTemplate:
      type: object
      properties:
        id: { type: string, format: uuid }
        key: { type: string, example: WELCOME }
        name: { type: string }
        subject: { type: string }
        bodyHtml: { type: string }
        availableVariables:
          type: array
          items: { type: string }
          description: "salt bilgi amaçlı, ör. [\"user_name\", \"reset_link\"]"
        updatedAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }

    UpdateEmailTemplateRequest:
      type: object
      properties:
        subject: { type: string, minLength: 1 }
        bodyHtml: { type: string }

    PreviewEmailTemplateRequest:
      type: object
      required: [sampleValues]
      properties:
        sampleValues:
          type: object
          additionalProperties: { type: string }

    PreviewEmailTemplateResponse:
      type: object
      properties:
        renderedSubject: { type: string }
        renderedHtml: { type: string }

    # ---- §10.8 Toplu İçe Aktarma (Import) ----

    ImportJobType:
      type: string
      description: >
        İçe aktarmanın HEDEFİ (dosya formatı değil — o `ImportSourceFormat`'tır).
        `WORDPRESS` tek bir WXR dosyasından hem `Page` hem `BlogPost` hem `BlogCategory`
        üretebilir; diğerleri tek hedeflidir.
      enum: [PAGES, BLOG, WORDPRESS, USERS, MEDIA]

    ImportSourceFormat:
      type: string
      description: >
        Sunucunun dosya İÇERİĞİNDEN türettiği format (istemci GÖNDERMEZ, gönderse de
        yok sayılır). Geçerlilik matrisi: PAGES,BLOG → CSV|JSON · WORDPRESS → XML ·
        USERS → CSV · MEDIA → ZIP.
      enum: [CSV, JSON, XML, ZIP]

    ImportJobStatus:
      type: string
      description: >
        - `PENDING` — dosya yüklendi ve ayrıştırıldı, önizleme hazır, KULLANICI ONAYI
        bekleniyor. Hiçbir kayıt YAZILMAMIŞTIR. 24 saat onaylanmazsa iş ve kaynak dosya
        otomatik temizlenir (tembel süpürme: her yeni yüklemede çalışır, cron YOK).

        - `QUEUED` — onaylandı, süreç-içi kuyrukta sırasını bekliyor (aynı anda tek iş).

        - `PROCESSING` — işleniyor; sayaçlar 25 kayıtta bir güncellenir.

        - `COMPLETED` — bitti. `errorCount > 0` OLABİLİR — kısmi başarı bir hata DEĞİLDİR
        (`POST /admin/pages/bulk` ile aynı felsefe).

        - `FAILED` — hiç kayıt yazılamadan çöktü (bozuk dosya, DB hatası) VEYA sunucu
        işlem sırasında yeniden başladı (bkz. `errorSummary`).

        - `CANCELLED` — kullanıcı iptal etti; o ana kadar yazılanlar KALIR.
      enum: [PENDING, QUEUED, PROCESSING, COMPLETED, FAILED, CANCELLED]

    ImportDuplicateStrategy:
      type: string
      description: >
        Var olan bir kayıtla çakışma bulunduğunda ne yapılacağı. Eşleştirme anahtarı:
        `PAGES`/`BLOG`/`WORDPRESS` → `slug` · `USERS` → `email` · `MEDIA` →
        `Media.filename`.


        - `skip` (VARSAYILAN) — mevcut kayıt korunur, satır `skippedCount`'a yazılır.

        - `overwrite` — mevcut kayıt güncellenir. Page/BlogPost'ta bu, normal `PATCH`
        gibi ÖNCE bir `ContentRevision` snapshot'ı yazar (§10.1) — geri dönüş mümkün
        kalsın diye. ÇÖPTEKİ (`deletedAt != null`) kayıt ASLA overwrite edilmez, atlanır
        (`TARGET_TRASHED`).

        - `createNew` — slug'a `-2`, `-3` … eklenerek yeni kayıt açılır. `USERS` ve
        `MEDIA` için GEÇERSİZDİR.


        **`USERS` için yalnızca `skip` kabul edilir; `overwrite`/`createNew` → 422.**
        Gerekçe: CSV ile mevcut bir kullanıcının rolünü değiştirebilmek doğrudan bir
        yetki yükseltme (privilege escalation) vektörüdür — rol değişikliği tek yolla,
        `PATCH /admin/users/{userId}/role` ile yapılır.
      enum: [skip, overwrite, createNew]

    CreateImportJobRequest:
      description: "`multipart/form-data` gövdesi — `POST /admin/import/jobs`."
      type: object
      required: [file, type]
      properties:
        file:
          type: string
          format: binary
          description: "Tek dosya. Boyut limitleri uç açıklamasındadır."
        type: { $ref: "#/components/schemas/ImportJobType" }

    ImportFieldMapping:
      description: >
        CSV/JSON içe aktarımında **kaynak alan adı → hedef şema alanı** eşlemesi
        (`{ "Baslik": "title", "Icerik": "contentHtml" }`). Anahtar = dosyadaki sütun/
        özellik adı, değer = hedef alan adı (`ImportJobPreview.targetFields` içindeki
        değerlerden biri) ya da `null` (bu sütunu YOK SAY).


        `WORDPRESS` ve `MEDIA` için ANLAMSIZDIR ve yok sayılır — WXR eşleştirmesi sabit
        ve kurallıdır (bkz. ARCHITECTURE.md §10.8 eşleştirme tablosu).
      type: object
      additionalProperties:
        type: string
        nullable: true

    ImportPreviewField:
      description: "Önizleme ekranındaki 'hangi alan eşleşti / eşleşmedi' satırı."
      type: object
      required: [sourceField, targetField, status]
      properties:
        sourceField:
          type: string
          description: "Dosyadaki sütun/özellik adı (CSV başlığı, JSON anahtarı, WXR eleman adı)."
        targetField:
          type: string
          nullable: true
          description: "Otomatik eşleşen hedef şema alanı; eşleşmediyse `null`."
        status:
          type: string
          description: >
            - `matched` — otomatik eşleşti (birebir ad veya bilinen eş anlamlı).

            - `unmatched` — hedefi bulunamadı; kullanıcı `fieldMapping` ile atayabilir,
            atamazsa bu sütun YOK SAYILIR (hata değildir).

            - `ignored` — bizim şemamızda karşılığı OLMADIĞI bilinen alan (ör. WordPress
            `post_tag`, `wp:comment`); UI bunu bilgi olarak gösterir.

            - `missingRequired` — ZORUNLU hedef alan (`title`; `USERS` için `email`)
            dosyada bulunamadı → iş başlatılamaz, `preview.canStart: false`.
          enum: [matched, unmatched, ignored, missingRequired]

    ImportJobPreview:
      description: >
        `POST /admin/import/jobs` sonrası dönen, ONAY EKRANINI besleyen özet. Yalnızca
        okuma amaçlıdır; hiçbir kayıt yazılmamıştır.
      type: object
      required: [totalCount, canStart, fields, targetFields, suggestedMapping, samples, warnings]
      properties:
        totalCount:
          type: integer
          description: "Dosyada bulunan İŞLENEBİLİR kayıt sayısı (atlanacağı şimdiden bilinenler hariç)."
        canStart:
          type: boolean
          description: >
            `false` ise `POST .../start` 422 döner (ör. zorunlu alan eşleşmedi, kayıt yok).
            UI onay butonunu bu alana göre pasifleştirir.
        fields:
          type: array
          items: { $ref: "#/components/schemas/ImportPreviewField" }
        targetFields:
          type: array
          description: >
            Bu `type` için atanabilecek hedef şema alanlarının listesi — eşleştirme
            dropdown'ını besler. Ör. `BLOG` → [title, slug, excerpt, contentHtml,
            coverImageUrl, status, categoryName, seoTitle, seoDescription, ogTitle,
            ogImageUrl, canonicalUrl, noIndex, publishedAt, authorEmail].
          items: { type: string }
        suggestedMapping:
          allOf:
            - $ref: "#/components/schemas/ImportFieldMapping"
          description: "Otomatik eşleşmenin sonucu — `StartImportJobRequest.fieldMapping` gönderilmezse bu kullanılır."
        samples:
          type: array
          description: "Dosyanın İLK 5 kaydı, eşleştirme uygulanmış hâliyle (kullanıcı gözle doğrulasın diye)."
          maxItems: 5
          items: { type: object, additionalProperties: true }
        breakdown:
          type: object
          description: >
            Yalnızca `WORDPRESS` için: WXR `wp:post_type` kırılımı. Frontend "12 sayfa,
            340 yazı, 87 medya bağlantısı, 5 kategori bulundu" özetini bundan üretir.
          properties:
            pages: { type: integer }
            posts: { type: integer }
            attachments: { type: integer }
            categories: { type: integer }
            skipped:
              type: integer
              description: "Desteklenmeyen `wp:post_type` (nav_menu_item, revision, custom_css, wp_block …) + `wp:status: trash|inherit`."
        warnings:
          type: array
          description: >
            İşi ENGELLEMEYEN ama kullanıcıya onaydan ÖNCE gösterilmesi ZORUNLU uyarılar.
            Kararlı kod + Türkçe metin (`SeoScoreIssue` ile aynı desen).
          items:
            type: object
            required: [code, message]
            properties:
              code:
                type: string
                enum:
                  - WP_MEDIA_NOT_DOWNLOADED
                  - WP_TAGS_UNSUPPORTED
                  - WP_AUTHOR_UNMATCHED
                  - WP_PRIVATE_AS_DRAFT
                  - WP_SCHEDULED_AS_DRAFT
                  - HTML_WILL_BE_SANITIZED
                  - SLUG_COLLISION
                  - MEDIA_SVG_REJECTED
                  - UNMAPPED_COLUMNS
              message: { type: string, description: "Gösterime hazır Türkçe metin." }
              count: { type: integer, description: "Kaç kaydı etkiliyor (varsa)." }

    ImportJobSummary:
      description: >
        `GET /admin/import/jobs` liste görünümü — `ImportJob`'ın `preview` alanı
        ÇIKARILMIŞ hâli (liste yanıtını şişirmemek için).
      type: object
      required: [id, type, format, status, filename, sizeBytes, totalCount, processedCount, successCount, errorCount, skippedCount, createdAt]
      properties:
        id: { type: string, format: uuid }
        type: { $ref: "#/components/schemas/ImportJobType" }
        format: { $ref: "#/components/schemas/ImportSourceFormat" }
        status: { $ref: "#/components/schemas/ImportJobStatus" }
        duplicateStrategy:
          allOf:
            - $ref: "#/components/schemas/ImportDuplicateStrategy"
          nullable: true
          description: "`PENDING` işlerde `null` (henüz seçilmedi)."
        filename:
          type: string
          description: >
            Kullanıcının yüklediği ORİJİNAL dosya adı — yalnızca GÖSTERİM içindir.
            Diskteki ad ilişkisizdir (rastgele UUID) ve API'de HİÇBİR ZAMAN dönmez.
        sizeBytes: { type: integer }
        totalCount: { type: integer, description: "Dosyadaki işlenebilir kayıt sayısı (`preview.totalCount` ile aynı)." }
        processedCount: { type: integer, description: "İşlenmiş kayıt = success + error + skipped. İlerleme çubuğu: processedCount / totalCount." }
        successCount: { type: integer }
        errorCount: { type: integer }
        skippedCount: { type: integer, description: "Çakışma nedeniyle (strateji `skip`) veya desteklenmeyen tür nedeniyle atlananlar." }
        errorSummary:
          type: string
          nullable: true
          description: "İşin TAMAMINI başarısız kılan hata (`FAILED`) için tek satırlık Türkçe açıklama; satır-bazlı hatalar burada DEĞİL `GET .../errors`'tadır."
        createdById: { type: string, format: uuid, nullable: true }
        createdBy:
          allOf:
            - $ref: "#/components/schemas/UserSummary"
          nullable: true
        createdAt: { type: string, format: date-time }
        startedAt: { type: string, format: date-time, nullable: true }
        finishedAt: { type: string, format: date-time, nullable: true }

    ImportJob:
      description: "Tekil iş DTO'su — `ImportJobSummary` + `preview`."
      allOf:
        - $ref: "#/components/schemas/ImportJobSummary"
        - type: object
          properties:
            preview:
              allOf:
                - $ref: "#/components/schemas/ImportJobPreview"
              nullable: true
              description: "İş sonlandıktan sonra da korunur (rapor ekranı geçmişte de açılabilsin diye)."

    StartImportJobRequest:
      description: "Onay ekranının seçimleri. Gövde HİÇ gönderilmezse tüm varsayılanlar uygulanır."
      type: object
      properties:
        fieldMapping:
          allOf:
            - $ref: "#/components/schemas/ImportFieldMapping"
          description: "Verilmezse `preview.suggestedMapping` kullanılır. `WORDPRESS`/`MEDIA`'da yok sayılır."
        duplicateStrategy:
          allOf:
            - $ref: "#/components/schemas/ImportDuplicateStrategy"
          description: "Varsayılan `skip`."
        defaultStatus:
          type: string
          enum: [DRAFT, PUBLISHED]
          description: >
            `PAGES`/`BLOG` içe aktarımında kaynakta `status` alanı YOKSA/BOŞSA uygulanacak
            varsayılan. **Varsayılanın varsayılanı `DRAFT`'tır** — içe aktarılan içerik
            gözden geçirilmeden public siteye düşmesin. `WORDPRESS`'te yok sayılır
            (orada `wp:status` bağlayıcıdır).
        defaultAuthorId:
          type: string
          format: uuid
          nullable: true
          description: >
            Yazarı çözümlenemeyen kayıtlara atanacak kullanıcı. Verilmezse `null` kalır
            (UI "—" gösterir). **İçe aktarma HİÇBİR KOŞULDA kendiliğinden kullanıcı
            OLUŞTURMAZ** — `USERS` içe aktarımı bunun tek istisnası ve tek yoludur.
        defaultCategoryId:
          type: string
          format: uuid
          nullable: true
          description: "`BLOG` (CSV/JSON) için kategorisi çözümlenemeyen yazılara atanacak kategori. `WORDPRESS`'te kategoriler WXR'dan upsert edilir, bu alan yok sayılır."

    ImportJobErrorCode:
      type: string
      description: >
        Kararlı, makine-okunur satır hatası kodu — frontend gruplama/rozet mantığı ve
        qa testleri BU KODA bağlanır, `message` yalnızca gösterimdir (`SeoScoreIssueCode`
        ile aynı sözleşme).


        **Bu liste yalnızca GERÇEKTEN tetiklenen kodları içerir.** Yazar/kategori
        çözümlenemediğinde satır hatası ÜRETİLMEZ — kayıt `defaultAuthorId`/
        `defaultCategoryId`'ye, o da yoksa `null`'a sessizce düşer ve BAŞARILI sayılır
        (bkz. ARCHITECTURE.md §10.8.7). Başarılı bir satır için "uyarı" üretmek
        `successCount`/`errorCount` özetini yanıltıcı kılardı; hata modelinde bilinçli
        olarak `WARNING` severity YOKTUR. Operatörün kontrolü `StartImportJobRequest`
        içindeki default alanlarıdır; WXR'da ayrıca onay ÖNCESİ agregat uyarı gösterilir
        (`ImportJobPreview.warnings[].code = WP_AUTHOR_UNMATCHED`).
      enum:
        - REQUIRED_FIELD_MISSING
        - INVALID_VALUE
        - INVALID_EMAIL
        - INVALID_ROLE
        - INVALID_DATE
        - INVALID_URL
        - DUPLICATE_SKIPPED
        - TARGET_TRASHED
        - SLUG_CONFLICT
        - UNSUPPORTED_POST_TYPE
        - UNSUPPORTED_STATUS
        - UNSUPPORTED_MIME
        - FILE_TOO_LARGE
        - EMAIL_DELIVERY_FAILED
        - DB_ERROR

    ImportJobError:
      description: >
        Tek bir kaydın başarısız olma/atlanma nedeni. **Atlama (`skipped`) da burada
        raporlanır** — kullanıcı "3 kayıt neden atlandı?" sorusunu tek listeden yanıtlar;
        `severity` alanı ikisini ayırır.
      type: object
      required: [id, rowNumber, code, message, severity, createdAt]
      properties:
        id: { type: string, format: uuid }
        rowNumber:
          type: integer
          description: >
            KAYNAK DOSYADAKİ 1-tabanlı sıra. CSV'de başlık satırı HARİÇ (ilk veri satırı
            = 1). JSON'da dizi indeksi + 1. WXR'da `<item>` sırası. ZIP'te arşiv içindeki
            giriş sırası.
        code: { $ref: "#/components/schemas/ImportJobErrorCode" }
        message: { type: string, description: "Gösterime hazır Türkçe açıklama." }
        severity:
          type: string
          enum: [error, skipped]
          description: "`error` → `errorCount`'a, `skipped` → `skippedCount`'a sayılır."
        field:
          type: string
          nullable: true
          description: "Hataya yol açan hedef alan adı (varsa), ör. `email`, `slug`."
        sourceRef:
          type: string
          nullable: true
          description: "Kaynaktaki tanımlayıcı — WXR'da `wp:post_id`, ZIP'te arşiv içi dosya adı, CSV/JSON'da varsa `slug`/`email`."
        rawData:
          type: object
          nullable: true
          additionalProperties: true
          description: >
            Satırın ham hâli (yeniden deneme/düzeltme için). **8 KB'a kırpılır** ve
            KİŞİSEL VERİ İÇEREBİLİR → ADMIN-only olması yeterli değildir, saklama süresi
            compliance-agent kararına tabidir (bkz. ARCHITECTURE.md §10.8).
        createdAt: { type: string, format: date-time }

    ImportJobErrorListMeta:
      description: "`GET /admin/import/jobs/{jobId}/errors` yanıtının `meta` zarfı."
      type: object
      required: [truncated]
      properties:
        nextCursor: { type: string, nullable: true }
        truncated:
          type: boolean
          description: >
            `true` ise iş başına 1.000 satırlık saklama tavanı aşılmıştır ve hata
            satırlarının bir kısmı DB'ye YAZILMAMIŞTIR (`ImportJob.errorCount` yine de
            gerçek toplamı gösterir). UI bunu açıkça belirtir.

    # ---- §10.8.10 Analitik Rapor Dışa Aktarma (Export) ----

    ExportJobType:
      type: string
      description: >
        Dışa aktarılan rapor kaynağı — ilgili `/admin/stats/*` ucuyla BİREBİR eşleşir
        (`TOP_CONTENT` → `GET /admin/stats/top-content`, vb.).
      enum: [VIEWS, BREAKDOWN, SUMMARY, TOP_CONTENT, USERS, REVENUE]

    ExportFileFormat:
      type: string
      enum: [CSV, PDF]

    ExportJobStatus:
      type: string
      description: >
        `ImportJobStatus`'tan daha sadedir (`QUEUED`/`CANCELLED` YOKTUR) — export
        satır-satır işlenen bir toplu içe aktarma değil, tek seferlik rapor üretimidir.


        - `PENDING` — iş oluşturuldu, worker sırasını bekliyor.

        - `PROCESSING` — rapor üretiliyor.

        - `COMPLETED` — dosya hazır, `GET .../download` ile indirilebilir.

        - `FAILED` — hiç dosya üretilmeden başarısız oldu (bkz. `errorSummary`).
      enum: [PENDING, PROCESSING, COMPLETED, FAILED]

    ExportFilters:
      type: object
      description: >
        Yalnızca ilgili `type` için anlamlıdır — diğer tiplerde gönderilse de worker
        tarafından YOK SAYILIR.
      properties:
        role:
          type: string
          enum: [ADMIN, EDITOR, VIEWER]
          description: "Yalnızca `type: USERS` için."
        subscriptionStatus:
          type: string
          enum: [TRIALING, ACTIVE, PAST_DUE, CANCELED, INCOMPLETE]
          description: "Yalnızca `type: REVENUE` için."

    CreateExportJobRequest:
      type: object
      required: [type, format, from, to]
      properties:
        type: { $ref: "#/components/schemas/ExportJobType" }
        format: { $ref: "#/components/schemas/ExportFileFormat" }
        from: { type: string, format: date-time, description: "ISO-8601 (UTC). `to` ile BİRLİKTE zorunludur." }
        to:
          type: string
          format: date-time
          description: "ISO-8601 (UTC). Aralık en fazla 366 gün olabilir ve `from` <= `to` olmalıdır — aşımı 422, iş OLUŞTURULMAZ."
        granularity:
          allOf:
            - $ref: "#/components/schemas/Granularity"
          description: "Varsayılan `day`. `type: TOP_CONTENT`/`VIEWS`/`BREAKDOWN` raporlarında worker tarafında yok sayılabilir (ilgili `/admin/stats/*` ucunun granularity kullanımıyla TUTARLI)."
        filters:
          allOf:
            - $ref: "#/components/schemas/ExportFilters"
          description: "Varsayılan `{}`."
        unmaskPii:
          type: boolean
          default: false
          description: >
            `true` verilirse `USERS`/`REVENUE` raporlarındaki PII alanları (ör. e-posta)
            MASKELENMEDEN dönülür. AYRI bir onay akışı YOKTUR (zaten ADMIN-only bir uç)
            ama `reports.export.unmasked_pii` audit kaydı yazılır, `ExportJob.containsPii`
            `true` set edilir ve dosyanın saklama süresi 7 günden 48 saate DÜŞER
            (compliance-agent kararı, bkz. ARCHITECTURE.md §10.8.10).

    ExportJob:
      type: object
      required: [id, type, format, status, filters, containsPii, errorSummary, createdById, expiresAt, startedAt, finishedAt, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        type: { $ref: "#/components/schemas/ExportJobType" }
        format: { $ref: "#/components/schemas/ExportFileFormat" }
        status: { $ref: "#/components/schemas/ExportJobStatus" }
        filters:
          type: object
          additionalProperties: true
          description: "İstek parametrelerinin tamamı (`from`/`to`/`granularity`/`filters`/`unmaskPii`) — bkz. `CreateExportJobRequest`."
        containsPii:
          type: boolean
          description: >
            `USERS`/`REVENUE` gibi kişisel veri içerebilecek türlerde `true` (maskeli
            export'larda DA `true`dur — maskeleme durumunu YANSITMAZ; ham/maskelenmemiş
            erişim yalnızca `reports.export.unmasked_pii` audit kaydıyla izlenir).
        errorSummary: { type: string, nullable: true }
        createdById: { type: string, format: uuid, nullable: true }
        createdBy:
          allOf:
            - $ref: "#/components/schemas/UserSummary"
          nullable: true
        expiresAt:
          type: string
          format: date-time
          nullable: true
          description: "İndirme linkinin/dosyanın süre sonu — bu süreden sonra `GET .../download` 404 döner."
        startedAt: { type: string, format: date-time, nullable: true }
        finishedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        # NOT: `storagePath` (gizli depodaki dosya referansı) BİLEREK burada YOK — API'de
        # ASLA dönmez, dosya yalnızca `GET .../download` üzerinden akıtılır.

    ExportJobListMeta:
      type: object
      properties:
        nextCursor: { type: string, nullable: true }
